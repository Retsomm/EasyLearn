// 純閱讀教材：跟 chapters.ts 的測驗章節不同，沒有關卡、沒有題目，
// 每個 section 是一段段完整的說明文字（翻譯改寫自官方文件），給想從零開始讀懂一個主題的人用。
// 「章節重點」畫面會分開列出這份清單跟 chapters.ts 的測驗章節，兩者互不影響。
// paragraphs 裡用反引號包住的片段（例如 `useState`）代表程式碼識別字，畫面用 renderInlineCode 解析成 code 樣式。
import type { ReadingChapter } from '../types'

export const readingChapters: ReadingChapter[] = [
  {
    id: 'react-native',
    title: 'React Native',
    icon: 'smartphone',
    sections: [
      {
        id: "rn-1",
        title: "什麼是 React Native",
        paragraphs: [
          "React Native 是由 Meta 開發、目前由社群持續維護的一套開放原始碼框架，讓你用 JavaScript 搭配 React 的寫法，就能做出真正跑在 iOS 跟 Android 上的手機 App。這裡的「真正」兩個字很重要，因為它跟很多人以為的「用網頁技術包一層殼」的做法完全不同，寫出來的畫面、操作手感，跟用 Swift 或 Kotlin 從零刻出來的原生 App 基本上沒有落差。",
          "如果你寫過網頁版的 React，會發現 React Native 用的核心觀念幾乎一模一樣：一樣是元件（component）、一樣寫 JSX、一樣有 props 跟 state。差別在於最後渲染出來的目標不一樣——網頁版 React 最終是把這些元件轉成瀏覽器看得懂的 HTML 標籤，React Native 則是把同一套元件轉成 iOS 跟 Android 各自認得的原生介面元件，兩者用的是同一種思考方式，落地的地方不同。",
          "很多人聽到「一套程式碼、兩個平台都能跑」，會誤以為 React Native 是把畫面畫成一模一樣的像素、硬塞進兩個系統裡。實際上它的精神比較接近「學一次、到處都能寫」：你用同一套 React 思維跟元件去描述畫面該長什麼樣、該怎麼互動，但 iOS 跟 Android 各自的介面細節、操作慣例還是保留下來，並不是逼兩個平台變成同一張畫面。",
          "技術上這件事是怎麼發生的？你寫的 JavaScript 邏輯會在自己的執行環境裡跑，透過一層溝通機制把「畫面應該長怎樣、該怎麼更新」這些訊息傳給原生那一層，最後實際負責畫出畫面的，是 iOS 跟 Android 系統本身內建的原生元件，而不是一個假裝成手機介面的網頁畫面（也就是不是用 WebView 模擬出來的）。這也是為什麼 React Native 做出來的 App 效能、手感能跟原生開發幾乎持平的原因。",
          "如果沒有 React Native，想同時做 iOS 跟 Android 兩個版本的 App，傳統做法是兩組人馬、兩套語言分開寫——iOS 用 Swift 或 Objective-C，Android 用 Kotlin 或 Java，同一個功能要寫兩次、修一個 bug 要改兩個地方。React Native 讓大部分的商業邏輯、畫面結構可以共用同一份程式碼，兩個平台只在真的需要客製化的地方才分開處理，開發跟維護的成本因此大幅降低。",
          "對已經會寫網頁版 React 的開發者來說，React Native 的吸引力又更明顯：不用重新學一套全新的語言跟框架，原本對元件、狀態管理、hooks 的理解幾乎可以直接搬過來用，只是要換一批新的元件名稱跟事件名稱。這也是為什麼很多網頁前端工程師會把 React Native 當作跨入手機開發最順手的入口。",
          "目前業界跟官方比較主流的做法，是搭配一個叫 Expo 的開發框架來寫 React Native，而不是從零自己搭建原生建置環境。用 Expo 的好處是你可以先專心寫應用邏輯，不用一開始就處理 Android Studio、Xcode 這些原生工具鏈的複雜設定，透過 Expo Go 這類工具，寫完程式碼馬上就能在手機或模擬器上看到即時效果，等真的需要更深入的原生功能時，再視情況調整開發方式即可，這讓新手入門的門檻降低不少。",
          "總結來說，React Native 的定位不是要取代 iOS 或 Android 的原生開發，也不是把網頁包裝成 App 的捷徑，而是提供一種接近寫網頁 React 的開發體驗，讓你用同一套技能產出貨真價實的原生 App。接下來的章節會依序帶你認識 React Native 裡有哪些核心元件可以用、React 的基本觀念在這裡怎麼運作，以及沒有 CSS 之後樣式該怎麼寫，這些都是從「React Native 到底是什麼」這個起點延伸出去的內容。",
        ],
      },
      {
        id: "rn-2",
        title: "核心元件與原生元件",
        paragraphs: [
          "在 React Native 裡畫畫面，不是用網頁那套 `div`、`p`、`img` 標籤，而是用一批內建、開箱即用的 React 元件，官方把它們統稱為「核心元件」（Core Components），像是 `View`、`Text`、`Image`、`ScrollView`、`TextInput` 都屬於這一類。這些元件就是你搭建任何一個畫面最基本的積木，之後不管做多複雜的介面，追根究柢都是這幾個元件互相組合、嵌套出來的結果。",
          "`View` 是最基本的容器元件，概念上跟網頁的 `div` 很像，但預設是不能捲動的，主要負責排版、承載樣式、處理部分的觸控互動跟無障礙設定。一個 React Native 畫面幾乎都是一層包一層的 `View` 疊出來的，可以說整個 App 的骨架都是靠 `View` 撐起來的。",
          "`Text` 元件負責顯示文字，這裡有一個網頁開發者容易不習慣的地方：在 React Native 裡，任何文字內容都一定要包在 `Text` 元件裡面才能顯示出來，不能像網頁那樣把文字隨便丟在容器裡。`Text` 可以設定樣式、可以互相嵌套（例如一段文字裡面再包一小段用不同樣式呈現的文字），也可以監聽點擊事件，功能上大致對應到網頁的 `p` 標籤。",
          "`Image` 元件用來顯示圖片，圖片來源可以是專案裡的本地檔案，也可以是網路上的圖片網址。跟網頁的 `img` 標籤不太一樣的地方是，`Image` 通常需要你自己明確指定寬高樣式，不會像網頁圖片那樣自動依照原始尺寸把容器撐開，這點在排版的時候要特別留意。",
          "`ScrollView` 是一個通用的可捲動容器，把多個元件塞進去之後，只要內容超出畫面範圍，使用者就能上下（或設定成左右）滑動瀏覽。`TextInput` 則是讓使用者輸入文字的元件，概念上對應到網頁的 `input` 標籤，但事件名稱不太一樣，例如監聽輸入內容變化用的是 `onChangeText`，而不是網頁常見的 `onChange`。",
          "這些核心元件之所以特別，是因為它們在執行的時候並不是用網頁技術或畫布模擬出來的假原生介面，而是 React Native 真的去呼叫 iOS 跟 Android 各自系統內建的原生 UI 元件。舉例來說，`View` 在 Android 上對應到的是 `ViewGroup`、在 iOS 上是 `UIView`；`Text` 對應到 Android 的 `TextView`、iOS 的 `UITextView`；`TextInput` 對應到 Android 的 `EditText`、iOS 的 `UITextField`。正因為背後真的是原生元件在運作，React Native 做出來的 App 不管是外觀質感還是效能表現，都能跟純原生開發的 App 幾乎一致。",
          "每個核心元件的外觀都是透過一個叫 `style` 的 prop 來設定，實務上通常會搭配 `StyleSheet.create` 把樣式集中管理，讓程式碼結構更清楚，這部分的細節會留到專門介紹樣式設定的章節再仔細說明，這裡先知道「元件的長相是靠 `style` 這個 prop 控制」就好。",
          "除了 React Native 內建的這些核心元件，社群也貢獻了大量第三方的原生元件套件，涵蓋地圖、相機、掃描條碼等各式各樣的功能。想找現成的解決方案時，可以到 Native Directory（網址是 `reactnative.directory`）這個社群維護的資源網站搜尋跟比較別人已經寫好、測試過的套件，不用每個功能都自己從零打造一個原生模組。",
          "把 `View`、`Text`、`Image`、`ScrollView`、`TextInput` 這幾個核心元件的用法跟對應關係搞清楚，等於打好了往後寫任何 React Native 畫面的地基。後面章節談到怎麼用 React 的方式組合元件、怎麼處理使用者輸入、怎麼設定樣式，都會建立在這一節認識的這幾個元件之上。",
        ],
      },
      {
        id: "rn-3",
        title: "從 React 過來：核心觀念對照",
        paragraphs: [
          "React Native 整個框架是建立在 React 之上的，所以如果你已經寫過網頁版的 React，這一節要介紹的元件（component）、JSX、props、state 這幾個核心觀念，對你來說完全是同一套東西，差別只在於最後畫出來的是手機的原生介面，而不是瀏覽器裡的 DOM。如果你是完全的新手，把這幾個觀念搞懂，會是接下來所有章節的基礎。",
          "先從元件講起：一個元件其實就是一個函式，負責回傳一段 JSX 來描述畫面應該長什麼樣子。舉個常見的例子，假設你寫一個叫做 `Cat` 的函式元件，裡面回傳一段 `Text` 顯示「Hello, I am your cat!」，這個 `Cat` 就成為一個之後可以在畫面任何地方重複拿來用的積木，不用每次都重新寫一次。",
          "JSX 則是讓你可以在 JavaScript 程式碼裡直接寫出類似 HTML 標籤語法的一種寫法，例如前面提到的 `Text` 標籤。如果想在 JSX 裡面塞進變數或是某個函式執行後的結果，就要用大括號 `{ }` 把它包起來，這個大括號可以理解成「暫時切回 JavaScript 世界」的入口，這一點跟網頁版 React 的寫法完全相同，沒有任何差異。",
          "核心元件之間可以互相嵌套，組合成新的、屬於自己的元件，例如在一個 `View` 容器裡面同時放進 `Text` 顯示文字、再放一個 `TextInput` 讓使用者輸入內容。當你的元件開始互相包來包去，就會出現父元件跟子元件的關係——負責渲染其他元件的叫父元件，被渲染進去的叫子元件，比方說一個 `Cafe` 元件裡渲染了好幾隻不同名字的 `Cat`，每隻 `Cat` 都是子元件，可以無限次重複使用而不用重複寫程式碼。",
          "props 是「properties（屬性）」的縮寫，作用是讓你從外部客製化一個元件顯示的內容，概念上很像函式的參數。例如寫成 `<Cat name=\"Garfield\" />` 這樣傳值進去，元件內部就能用 `props.name` 把這個名字取出來顯示。這裡有個小細節：傳字串不用加大括號，但傳數字、陣列、物件這些型別的值就一定要用大括號包起來；因為物件本身在 JavaScript 裡就是用大括號表示，所以像傳樣式物件這種情況，畫面上常常會看到雙層大括號，例如 `style={{width: 200}}`，第一層是「切回 JS」的入口，第二層才是物件本身的大括號。",
          "props 是外部給的、屬於靜態設定的資料，state 則不一樣，它是元件自己記住、而且預期會隨時間改變的資料，例如「這隻貓現在肚子餓不餓」這種狀態。要讓一個函式元件擁有這種會變動的記憶，要用 `useState` 這個 Hook：呼叫 `useState(true)` 之後會拿到一個目前的值跟一個用來更新它的函式，例如寫成 `const [isHungry, setIsHungry] = useState(true)`，之後只要呼叫 `setIsHungry(false)`，元件就會依照新的狀態重新渲染畫面，按鈕上的文字、能不能被點擊都可以跟著這個狀態一起連動。",
          "`useState` 只是眾多 Hook 裡最常見的一個，Hook 泛指這一類讓你「鉤進」React 內部功能的函式，讓原本單純回傳畫面的函式元件，也能擁有狀態、也能處理其他更進階的行為，而不需要改寫成比較繁瑣的 class 元件寫法。這整套 Hooks 的用法跟寫網頁版 React 時完全一樣，沒有額外要重新學的地方。",
          "那 React Native 跟網頁版 React 到底差在哪裡？元件、JSX、props、state、Hooks 這些核心觀念完全相同，差異主要在「用來呈現畫面的元件」跟「互動的事件名稱」上：網頁寫 `div`、`p`、`img` 這些 HTML 標籤，監聽點擊用 `onClick`、監聽輸入變化用 `onChange`；React Native 則是寫 `View`、`Text`、`Image` 這些核心元件，監聽點擊改用 `onPress`、監聽文字輸入變化改用 `onChangeText`。版面配置也不能用網頁常見的 Grid 或其他排版方式，幾乎都得靠 Flexbox 處理，這部分會在樣式設定的章節繼續深入說明。",
          "如果你原本就熟悉網頁版 React，這一節其實只是幫你把既有的知識重新貼上手機開發的標籤，核心觀念完全不用重學，換的只是一批元件名稱跟事件名稱；如果你是第一次接觸 React 系列的框架，只要先把元件、JSX、props、state 這四個觀念理解清楚，後面不管讀到哪個章節，理解起來都會順暢很多。",
        ],
      },
      {
        id: "rn-4",
        title: "樣式設定（Style）",
        paragraphs: [
          "React Native 沒有 CSS 檔案可以寫，所有的樣式都要用 JavaScript 物件來表示。寫法上跟 CSS 概念很接近，但屬性名稱一律改用駝峰式命名，例如 CSS 裡的 `background-color` 在這裡要寫成 `backgroundColor`，`text-align` 要寫成 `textAlign`。這是從網頁轉過來的開發者第一個要適應的地方——邏輯沒變，只是拼寫規則變了。",
          "每一個核心元件都會吃一個叫 `style` 的 prop，你可以直接塞一個 JavaScript 物件進去，例如寫成 `<View style={{backgroundColor: 'white', padding: 10}}>`，物件裡的每一組鍵值對就代表一條樣式規則。概念上跟網頁行內樣式 `style=\"...\"` 很像，只是從字串變成了正規的 JS 物件。",
          "當元件數量變多、樣式變複雜之後，把樣式物件全部直接寫死在 JSX 裡面會讓程式碼變得很亂，所以官方建議用 `StyleSheet.create` 這個方法，把一整批樣式集中定義成一個物件、幫每一組樣式取一個名字，之後畫面上就用 `style={styles.container}` 這樣的方式取用。這麼做能讓「畫面結構」跟「樣式定義」分開放，程式碼讀起來跟維護起來都輕鬆很多，也是實務上最常見的寫法。",
          "`style` 這個 prop 不是只能塞單一個物件，也可以塞一個陣列進去，例如寫成 `style={[styles.base, styles.highlight]}`。React Native 會依照陣列裡的順序把每個物件的屬性依序合併，後面出現的樣式如果跟前面有同名的屬性，會直接蓋掉前面的設定。這個技巧很常被用在「先套用一份基本樣式，再依照某個條件疊加一份額外樣式」的情境，例如某個狀態成立時多套一個變色效果。",
          "跟 CSS 相比，React Native 有一個很大的差異是沒有樣式繼承。CSS 裡父層設定的字體顏色、字體大小，子元素預設會自動繼承下去；但在 React Native，每個元件的樣式幾乎都得自己明確指定，沒有這種自動往下傳遞的機制。唯一的例外是 `Text` 元件——如果 `Text` 裡面又嵌套了另一段 `Text`，內層的文字會繼承外層 `Text` 設定的樣式，但這個繼承只發生在 `Text` 跟 `Text` 之間，像 `View` 這種容器元件並不會把自己的樣式傳給裡面包著的子元件。",
          "數字類型的樣式屬性，例如寬度、高度、間距，在 React Native 裡不需要加上 `px` 這種單位，直接寫一個純數字就好。這個數字代表的是「與螢幕密度無關的像素」（density-independent pixels），系統會依照每支裝置實際的螢幕密度自動換算成真正的物理像素，讓同一個數字在不同解析度的手機上呈現出來的視覺大小是一致的。也因為這樣，React Native 幾乎不需要像 CSS 那樣去煩惱 `rem`、`em`、`vw` 這些相對單位的換算問題。",
          "版面排版方面，React Native 沒有 CSS 的 `float`、Grid 這類排版工具可以用，畫面的版面配置幾乎都得靠 Flexbox 完成。這裡有個容易踩到的細節：React Native 裡 `flexDirection` 的預設值是 `column`（由上往下直向排列），跟網頁版 CSS Flexbox 預設的 `row`（橫向排列）方向不同，剛從網頁轉過來的人常常在這裡卡住，遇到排版跟預期不一樣時可以先檢查這個預設方向的差異。",
          "官方文件也特別提醒幾個跟 CSS 行為不一致、容易讓人誤踩的地方：觸控可以點擊的範圍，永遠不會超出父層元件的邊界，不像網頁有時候視覺上溢出的內容還是可以點得到；另外 Android 平台並不支援設定負的 `margin` 值，這在網頁 CSS 裡是很常見的排版技巧，但搬到 React Native 的 Android 環境就會失效，需要改用別的方式達到類似的效果。",
          "整體來說，React Native 的樣式系統可以理解成把 CSS「用鍵值對描述外觀」的精神，搬進了 JavaScript 物件裡，同時拿掉了自動繼承、拿掉了部分 CSS 專屬的排版語法，換上 `StyleSheet.create` 做集中管理、用陣列合併做樣式疊加當作替代方案。只要習慣這套邏輯轉換，寫 React Native 的樣式其實比想像中直覺，而且因為全部都是 JavaScript，還能享受變數、函式帶來的彈性，這是純 CSS 比較難做到的地方。",
        ],
      },
      {
        id: "rn-5",
        title: "高度與寬度",
        paragraphs: [
          "在 React Native 裡，一個元件在畫面上到底佔多大的空間，最終都是由它的 `height` 跟 `width` 這兩個屬性決定的。跟網頁前端不太一樣的地方在於，RN 裡的尺寸數字全部都是「無單位」的，你寫 `100` 並不是 `100px`，而是所謂的「density-independent pixels」（密度無關像素），這個概念的用意是讓同一組數字在不同解析度的手機螢幕上，看起來的物理大小盡量接近，而不是螢幕解析度越高、東西就顯得越小。理解這件事很重要，因為之後你在調整版面的時候，會發現同一個數字在不同裝置上呈現的「實際公分大小」還是會有些微差異，這是正常現象，官方文件也坦承目前沒有辦法在所有裝置上做到完全一致的物理尺寸對應，只是這個差異在大多數情況下小到可以忽略。",
          "第一種、也是最直覺的設定尺寸方式，叫做「固定尺寸」（Fixed Dimensions），做法就是直接在 `style` 裡面寫死 `width` 跟 `height` 的數字，例如 `width: 100`、`height: 100`。這種寫法適合用在「不管畫面多大、這個元件就是要維持固定大小」的情境，舉例來說，一個 icon 圖示、一個固定尺寸的頭像框、或是一個裝飾用的色塊，這些東西的大小通常跟螢幕寬窄無關，所以直接寫死數字最單純也最好掌握。官方範例示範了三個疊在一起的 `View`，分別給 `50`、`100`、`150` 的寬高，並且分別塗上 `powderblue`、`skyblue`、`steelblue` 三種藍色系背景色，畫面上就會呈現三個尺寸依序放大的正方形色塊，這是最快能感受到「固定尺寸」意義的方式。",
          "但固定尺寸有個很明顯的限制：如果你想做的是「這個區塊要填滿剩下的所有空間」，或是「兩個區塊要平分整個畫面」，用寫死數字的方式就會非常不靈活，螢幕換一台馬上就跑版。這時候就要換成第二種做法，也就是「彈性尺寸」，靠的是 `flex` 這個屬性。`flex` 的概念是讓元件可以隨著「可用空間」動態伸縮，而不是自己認定一個固定大小。最常見的寫法是 `flex: 1`，意思是「把所有可用空間，平均分給跟我同一層、也有設 `flex` 的兄弟元件」。如果 `flex` 的值越大，代表分到的空間比例就越高，這是一個相對值而不是絕對值，重點在於「跟其他兄弟元件比起來」分到多少比例。",
          "官方給的例子很經典：一個外層 `View` 設定 `flex: 1`（讓它填滿父層給的所有空間），裡面包了紅、橘、綠三個子 `View`，分別給 `flex: 1`、`flex: 2`、`flex: 3`。因為 1+2+3 等於 6，所以紅色會拿到 6 分之 1 的空間，橘色拿到 6 分之 2，綠色拿到 6 分之 3，也就是綠色會是紅色的三倍大。這個「比例分配」的邏輯，跟 CSS Flexbox 網頁開發者熟悉的 `flex-grow` 概念其實很類似，如果你有網頁前端背景，可以直接把它想成同一套邏輯搬到手機 App 上使用。",
          "不過使用 `flex` 尺寸的時候，有一個新手很容易忽略、卻非常關鍵的陷阱：一個元件要能夠用 `flex` 撐開填滿空間，前提是它的「父層」本身要有大於 0 的實際尺寸。也就是說，如果最外層的容器沒有設定固定的 `width/height`，也沒有設定 `flex`，那麼這個父層本身的尺寸就會是 0，即使你在它的子元件上寫了 `flex: 1`，子元件也不會顯示出來，因為「填滿空間」的前提「有空間可以填」根本不成立。所以每次遇到「畫面上什麼都沒顯示、明明有設 `flex` 卻整個消失」的狀況，第一件事就該去檢查：這一整條父子鏈往上找，有沒有哪一層漏了給尺寸或漏了給 `flex`，導致整條鏈斷掉。",
          "第三種尺寸設定方式是「百分比尺寸」，適合用在「我知道自己想佔螢幕的幾成空間，但又不想用 `flex` 那種比例分配邏輯」的情境。做法是把 `width` 或 `height` 直接寫成字串型態的百分比，例如 `height: '50%'`，代表這個元件的高度是它父層高度的一半。跟 `flex` 尺寸一樣，百分比尺寸也有相同的前提限制：父層必須要有一個明確的尺寸基準，不然「50% 的父層高度」根本無從算起，畫面一樣會什麼都不顯示。官方範例示範了一個父層設定 `height: '100%'`，底下三個子元件分別用 `height: '15%'`、`'35%'`、`'50%'` 三種比例切分空間，其中第二、三個子元件還額外搭配 `width: '66%'` 跟 `width: '33%'`，示範寬跟高可以各自獨立用百分比控制，不一定要綁在一起。",
          "當你需要在程式碼裡「讀出」目前手機螢幕實際有多少像素寬高的時候，React Native 提供了 `Dimensions` 這個模組，最常見的寫法是呼叫 `Dimensions.get('window')`，它會回傳一個包含 `width`、`height`、`scale`（螢幕像素密度）、`fontScale`（使用者系統字體縮放倍率）的物件，讓你可以在程式裡動態依據螢幕大小做版面判斷，比方說「螢幕寬度小於某個數值就切換成單欄排版」這類邏輯。要特別注意的是，這裡的 `window` 指的是應用程式視窗的大小，如果你想拿到的是整台裝置螢幕的大小（在 Android 上兩者可能因為狀態列、導覽列而有差異），可以改用 `Dimensions.get('screen')`。",
          "但 `Dimensions.get()` 有一個常被新手忽略的陷阱：它回傳的是「呼叫當下」那一瞬間的數值，並不會在螢幕尺寸改變的時候自動幫你更新。如果使用者把手機從直向轉成橫向、或是使用可折疊螢幕的裝置把螢幕展開／收合，你原本抓到的寬高數字就會過期失真，但畫面卻不會自動重新渲染去反映新的尺寸。正因為這樣，官方文件特別提醒：如果你的排版邏輯或樣式需要依賴螢幕尺寸，最好每次渲染都重新呼叫 `Dimensions.get()` 去讀取最新值，而不是只呼叫一次就把數字快取起來到處用；如果真的需要「螢幕轉向或尺寸變化時自動監聽並更新」，`Dimensions` 模組本身也提供了 `addEventListener('change', handler)` 這個方法，可以在尺寸變動時收到通知並執行回呼。",
          "不過在實際開發上，官方目前更推薦的做法是改用 `useWindowDimensions` 這個 React Hook，而不是手動呼叫 `Dimensions.get()` 再自己管理監聽器。`useWindowDimensions` 的好處是它會自動幫你訂閱視窗尺寸的變化，只要螢幕寬高或系統字體縮放倍率一有變動，使用這個 Hook 的元件就會自動重新渲染、拿到最新的 `width`、`height`、`scale`、`fontScale` 四個數值，完全不用你手動加監聽、也不用擔心忘記清除監聽器造成的記憶體洩漏問題。寫法非常單純，就是在元件裡呼叫 `const { width, height } = useWindowDimensions()`，這種「跟著 React 生命週期自動更新」的特性，讓它更符合 React 開發者習慣的宣告式寫法，也是目前撰寫新程式碼時官方建議優先採用的方式。",
        ],
      },
      {
        id: "rn-6",
        title: "用 Flexbox 排版面",
        paragraphs: [
          "React Native 裡元件要怎麼排列版面，靠的是一套叫做 Flexbox 的排版演算法，這套邏輯的設計目的就是要讓同一份版面邏輯，可以在手機、平板等各種不同尺寸的螢幕上都維持一致、可預期的排列結果。如果你之前寫過網頁前端、對 CSS 的 Flexbox 已經有基本概念，那麼恭喜你，這邊大部分的觀念都可以直接沿用；但如果你完全沒接觸過 Flexbox，也不用擔心，因為手機排版本來就是 RN 開發的核心日常，這套東西學會之後幾乎每個畫面都會用到。實務上你會最常混合搭配三個屬性來完成大部分版面需求：`flexDirection`（決定排列方向）、`justifyContent`（決定主軸上的對齊方式）、`alignItems`（決定交叉軸上的對齊方式），先把這三個搞熟，其他屬性都是在這個基礎上做微調。",
          "這裡有一個對網頁開發者來說最容易踩雷、也是官方文件特別用警語標出來的地方：React Native 的 Flexbox 雖然跟網頁 CSS 的 Flexbox 原理相同，但預設值不一樣。網頁 CSS 裡 `flexDirection` 預設是 `row`（水平排列），RN 裡卻預設是 `column`（垂直排列）；另外 `alignContent` 網頁預設是 `stretch`，RN 預設是 `flex-start`；`flexShrink` 網頁預設是 `1`，RN 預設卻是 `0`；而且 RN 裡的 `flex` 屬性只支援單一數字，不像網頁 CSS 的 `flex` 簡寫可以同時吃 `flex-grow`、`flex-shrink`、`flex-basis` 三個值。如果你是從網頁背景轉過來，這幾個預設值的差異一定要牢記，不然很容易寫出「明明程式碼看起來沒錯，畫面卻跟預期不一樣」的排版錯亂。",
          "先講 `flex` 這個屬性本身：它決定的是一個元件要如何在「主軸」方向上，跟兄弟元件一起分配可用空間，分配的邏輯是按照每個元件各自的 `flex` 數值比例去切。官方舉的例子是一個容器設定 `flex: 1`（先讓自己填滿父層給的空間），底下紅、橘、綠三個子元件分別設定 `flex: 1`、`flex: 2`、`flex: 3`，因為總和是 6，所以紅色拿 6 分之 1 空間、橘色拿 6 分之 2、綠色拿 6 分之 3，也就是綠色的大小會是紅色的三倍。這種「比例分配」的思維，會是你在 RN 裡切版面時最基本的直覺依據。",
          "接著是 `flexDirection`，它決定容器裡的子元件要沿著哪個方向排列，這個方向被稱為「主軸」，而垂直於主軸的方向則叫「交叉軸」。它有四個可能值：`column` 是預設值，代表由上往下排列，如果搭配換行（`wrap`）機制，換行後的下一批元件會排到第一個元件的右邊；`row` 則是由左往右排，換行後下一批會排到第一個元件的下方；`column-reverse` 跟 `row-reverse` 則分別是前兩者的反方向排列。因為 RN 預設就是 `column`，所以你會發現「畫面上東西由上往下一個個疊著排」是最原始、什麼都不設定時的自然狀態，如果想要「並排」而不是「疊放」，就要主動加上 `flexDirection: 'row'`。",
          "跟排列方向息息相關的還有一個叫 `direction` 的屬性，它管的是「文字書寫方向」，預設是 `LTR`（Left To Right，由左至右），這個模式下 `start` 對應到左邊、`end` 對應到右邊；如果切成 `RTL`（Right To Left，例如阿拉伯文、希伯來文這類語言的書寫習慣），`start` 跟 `end` 對應的實際方向就會整個左右互換。這個屬性平常在只做繁體中文內容的 App 裡比較少直接用到，但如果之後你的 App 需要支援多國語系、尤其是右到左書寫的語言，就會需要靠這個屬性讓版面自動翻轉方向。",
          "`justifyContent` 管的是子元件在「主軸」方向上怎麼對齊，它的可能值包括：`flex-start`（預設值，靠主軸起點對齊）、`flex-end`（靠主軸終點對齊）、`center`（置中）、`space-between`（子元件之間平均分配剩餘空間，但頭尾兩端不留空隙）、`space-around`（頭尾兩端也留空隙，但份量只有中間間隔的一半）、`space-evenly`（讓包括頭尾在內的每一段間隔都完全相等）。舉例來說，如果 `flexDirection` 是 `row`，`justifyContent` 設成 `center`，效果就是讓一整排子元件整體「水平置中」；如果 `flexDirection` 是 `column`，同樣的 `center` 效果就會變成「垂直置中」，這也是為什麼理解主軸方向這麼重要——同一個屬性的視覺效果，會因為 `flexDirection` 不同而整個改變。",
          "`alignItems` 則是負責交叉軸方向的對齊，概念上跟 `justifyContent` 很像，只是作用的軸不同。它的預設值是 `stretch`，意思是子元件會被自動拉伸去填滿容器在交叉軸方向的整個尺寸；其他可選值包括 `flex-start`、`flex-end`、`center`，意義跟 `justifyContent` 對應的選項相同，只是換算到交叉軸上；還有一個 `baseline`，可以讓子元件依照文字基線對齊，適合用在需要對齊不同字體大小文字的排版情境。這裡有個容易忽略的小細節：`stretch` 要真正發揮作用，子元件本身不能在交叉軸方向上寫死尺寸，比方說子元件如果自己設了 `width: 50`，`stretch` 就不會生效，因為子元件已經有明確尺寸，沒有「可以被拉伸」的空間了。",
          "如果你只想讓某一個子元件單獨跳脫父層 `alignItems` 的規則、有自己專屬的對齊方式，可以用 `alignSelf` 這個屬性，寫法上跟 `alignItems` 完全一樣（一樣可以設 `stretch`、`flex-start`、`center` 等），差別只在於它是套用在單一子元件上、而且會覆蓋掉父層 `alignItems` 的設定，這在「大部分子元件都要一起置中，只有某一個要靠右對齊」這種情境特別好用。另外還有一個 `alignContent` 屬性，它只有在搭配 `flexWrap` 讓內容換行成多列（或多行）的時候才有意義，管的是「這些換行後的每一整條線，彼此之間要怎麼對齊分佈」，選項跟 `justifyContent` 很像，包括 `flex-start`（預設）、`flex-end`、`center`、`stretch`、`space-between`、`space-around`、`space-evenly`。",
          "`flexWrap` 這個屬性決定的是：當子元件加起來的總尺寸超過容器在主軸方向的空間時該怎麼辦。預設情況下，RN 會硬是把所有子元件塞進同一行（可能導致元件被擠壓縮小），如果你希望超出的部分自動換到下一行，就要把 `flexWrap` 設成 `wrap`，這時候前面提到的 `alignContent` 才會開始發揮作用，去控制換行後每一條線的排列方式。除此之外還有三個比較細緻的比例控制屬性：`flexBasis` 用來設定元件在主軸方向的「預設基準尺寸」（在還沒套用 `flexGrow` / `flexShrink` 的伸縮計算之前，元件本來的大小）；`flexGrow` 決定容器裡剩餘空間要怎麼依照各子元件的權重分配出去，預設是 `0`（不主動長大）；`flexShrink` 則是反過來，決定當子元件全部塞不下容器時要怎麼依照權重收縮，RN 裡的預設值是 `0`，這點跟網頁 CSS 預設是 `1` 不同，也是切版時容易搞混的地方。最後，如果需要在排列的子元件之間留固定間距，可以直接用 `gap`（同時設定列與欄的間距）、或分開用 `rowGap` 跟 `columnGap`，不需要再手動幫每個子元件加 `margin` 去湊間距。",
        ],
      },
      {
        id: "rn-7",
        title: "處理文字輸入",
        paragraphs: [
          "在 React Native 裡，只要畫面上需要讓使用者打字輸入內容，不管是登入畫面的帳號密碼欄位、搜尋框，還是留言輸入區，都是靠 `TextInput` 這個核心元件來實現的。`TextInput` 本身提供了兩個最基本、幾乎每次使用都會用到的屬性：一個是 `onChangeText`，這是一個回呼函式，只要使用者打的文字內容有任何變動（每打一個字、每刪一個字）就會被呼叫一次，並且把「當下最新的完整字串」當作參數傳進來；另一個是 `onSubmitEditing`，它會在使用者按下輸入法的「送出」或「完成」鍵時被觸發，適合用在「按下 Enter 就直接送出這則訊息」這類情境。要特別留意的是，官方文件也提到在 iOS 上，如果 `keyboardType` 設定成 `phone-pad`，`onSubmitEditing` 並不會被呼叫，這是因為電話鍵盤本身沒有標準的送出鍵。",
          "React Native 官方文件用一個很生動的例子來說明怎麼串接 `TextInput` 跟畫面上其他內容：假設你要做一個「翻譯機」，使用者打的每一個字都會被翻譯成同一個 pizza 表情符號 🍕，所以如果使用者輸入 Hello there Bob，畫面上就要顯示三個 🍕 圖案（因為原句是三個單字）。要做到這件事，程式裡需要先用 `useState` 建立一個狀態變數來存目前使用者打的文字內容，因為這段文字會隨著使用者打字持續改變，屬於「會變動」的資料，本來就該放進 React 的 state 裡管理，而不是放在一般變數裡（放一般變數畫面不會跟著重新渲染）。",
          "具體寫法上，畫面裡會放一個 `TextInput`，給它 `placeholder` 屬性顯示「`Type here to translate!`」這樣的提示文字，`onChangeText` 裡呼叫 `setText(newText)` 把打字內容存進 state，並且用 `value={text}` 讓輸入框顯示目前的文字內容；接著下面再放一個 `Text` 元件，把 state 裡的字串用空白切開、每個單字都換成 🍕 圖案、再用空白重新接起來顯示出來。這種「輸入框跟畫面上其他元件共用同一份 state」的寫法，在 React 的世界裡有個專有名詞叫做「受控元件」（controlled component），意思是輸入框顯示的內容完全「受」你程式裡的 state 控制，而不是自己管理內部狀態，這樣你才能很方便地在使用者打字的當下就同步去做各種處理，例如即時驗證格式、即時轉換內容、或是把內容同步到畫面別處。",
          "這裡值得多解釋一下 `value` 跟 `defaultValue` 這兩個容易搞混的屬性。如果你把 `value` 屬性綁到 state 上，`TextInput` 就會變成完全受控：畫面上顯示的文字，永遠會被強制對齊你傳進去的 `value` 值，即使使用者打字，最終顯示出來的內容也是由你的 state 決定，這給你最大的掌控權，但相對也要求你自己確實在 `onChangeText` 裡把新的文字存回 state，不然就會遇到「打字打不進去」的狀況。而 `defaultValue` 則是「只提供一個初始值」，之後使用者打字時，輸入框內部會自己管理接下來的顯示內容，不需要你手動同步 state 就能正常打字，適合用在「你只是想要讀取使用者最終打了什麼，但不需要在打字過程中即時介入畫面顯示」的情境，可以少寫一些同步邏輯。",
          "如果你的輸入框需要讓使用者輸入超過一行的內容，例如留言框、意見回饋欄位，就要用到 `multiline` 這個屬性，把它設成 `true` 之後，`TextInput` 就會從單行輸入變成可以換行的多行文字區塊。這裡有個平台差異要注意：多行模式下，iOS 預設會把文字對齊到輸入框頂端，但 Android 預設卻是垂直置中對齊，如果你希望兩個平台看起來一致，可以額外加上 `textAlignVertical: 'top'` 的樣式去統一對齊方式。另外還有一個 `numberOfLines` 屬性，可以搭配 `multiline` 一起使用，用來設定輸入框最多要顯示幾行的高度。",
          "鍵盤相關的設定，最常用的就是 `keyboardType`，它可以決定使用者點擊輸入框時，系統會跳出哪一種樣式的鍵盤。常見的跨平台選項包括：`default`（一般文字鍵盤）、`numeric`（數字鍵盤）、`number-pad` 跟 `decimal-pad`（純數字或含小數點的鍵盤）、`email-address`（會把鍵盤上的符號鍵排列調整成方便輸入 email 的樣子，例如把 `@` 符號放在顯眼位置）、`phone-pad`（電話號碼專用鍵盤）、`url`（方便輸入網址）。選對 `keyboardType`，可以大幅減少使用者輸入時需要切換鍵盤面板的麻煩，是很值得養成的小習慣。",
          "涉及密碼或其他敏感資訊輸入的時候，要記得加上 `secureTextEntry` 屬性並設為 `true`，這樣使用者打的每個字都會被畫面上的圓點或星號遮住，避免旁人偷看到內容；不過要注意，這個屬性沒辦法跟 `multiline` 同時使用，密碼框本來就應該是單行輸入。另外還有兩個很實用的文字處理屬性：`autoCapitalize` 可以控制系統是否要自動幫使用者把某些字母轉成大寫，選項包括 `sentences`（每個句子開頭大寫，這是預設值）、`words`（每個單字開頭大寫）、`characters`（每個字母都大寫）、`none`（完全不自動大寫）；`autoCorrect` 則是控制要不要開啟系統的自動校正／拼字建議功能，預設是開啟（`true`），如果是輸入帳號、驗證碼這類不希望被系統自動「修正」的內容，通常會把它關掉。",
          "跟輸入框的互動狀態相關，還有幾個常用屬性：`autoFocus` 可以讓畫面一出現，這個輸入框就自動取得焦點、直接彈出鍵盤，適合像搜尋頁這種「一進來就是要打字」的畫面；`editable` 設成 `false` 可以讓輸入框變成唯讀、無法編輯，常用在顯示唯讀資訊或表單審核中鎖定欄位的情境；`onFocus` 跟 `onBlur` 則分別是輸入框「取得焦點」跟「失去焦點」時會被呼叫的回呼函式，可以用來做像是「輸入框被點擊時外框變色強調」這類互動效果。如果需要限制輸入長度，例如驗證碼固定六碼、暱稱不能超過二十字，建議直接用 `maxLength` 屬性去限制，而不是自己在 JavaScript 邏輯裡手動判斷字數再截斷，因為用 `maxLength` 讓原生層直接擋住超過長度的輸入，可以避免自己手動處理時常見的畫面「先打進去、又被程式砍掉」的閃爍問題。",
          "`TextInput` 只是使用者跟 App 互動的其中一種方式，官方文件也特別提醒，如果你想瞭解使用者「點按、觸控」這類手勢的處理方式（而不是打字輸入），可以進一步參考「處理觸控」這個主題，裡面會介紹 `Button` 元件跟一系列 `Touchable` 系列元件的用法。把「文字輸入」跟「觸控手勢」這兩塊搞清楚之後，你就掌握了 RN 裡使用者輸入資料最核心的兩種管道，之後不管是做登入表單、搜尋框，還是各種需要蒐集使用者資料的畫面，都是把這些基礎元件跟屬性排列組合出來的結果而已。",
        ],
      },
      {
        id: "rn-8",
        title: "處理觸控",
        paragraphs: [
          "手機 App 跟使用者互動的方式，絕大多數都是透過觸控完成的：點一下按鈕、滑動捲動清單、兩指縮放地圖，這些都是觸控手勢的一種。React Native 針對這些常見手勢都提供了對應的元件可以直接使用，背後其實還有一套更完整的「手勢響應系統」（gesture responder system）在支撐，讓你有需要的話可以做更進階的自訂手勢辨識，不過對大多數初學情境來說，你最先會用到、也最常用到的，其實就是最基本的 `Button` 元件。",
          "顯示一個最簡單的按鈕，只需要用 `Button` 元件並給它兩個屬性：`title`（按鈕上顯示的文字）跟 `onPress`（使用者點下去要執行的函式）。這個元件的好處是它「開箱即用」，不需要額外寫樣式就能在 iOS 跟 Android 上呈現出符合各自平台介面規範、看起來自然的按鈕外觀——在 iOS 上會顯示成一個藍色文字連結的樣式，在 Android 上則會顯示成一個藍底白字的圓角矩形按鈕。你也可以額外加上 `color` 這個屬性去改變按鈕的顏色，但除此之外 `Button` 能客製化的空間非常有限，這也是它最大的限制所在。",
          "正因為 `Button` 元件的外觀客製彈性很小——你沒辦法幫它換字體、加圖示、調整內距、或是做出漸層背景之類比較講究的設計——一旦你的 App 對按鈕的視覺呈現有比較明確的品牌風格要求，`Button` 往往就不夠用了。這時候就要改用官方所謂的「`Touchable`」系列元件，這些元件的定位剛好跟 `Button` 相反：它們完全不附帶任何預設外觀樣式，一切排版、顏色、形狀都要你自己用 `style` 屬性去設計，但相對的，它們願意把「偵測使用者點按」跟「點按當下要呈現什麼視覺回饋」這兩件事的主導權完全交給你決定，讓你可以做出真正符合自己設計稿的自訂按鈕。",
          "`Touchable` 系列元件之間最主要的差異，就在於「使用者按下去的當下，要給什麼樣的視覺回饋」，選哪一個完全看你想要的效果。`TouchableHighlight` 的效果類似網頁上常見的按鈕或連結：使用者按下去的時候，元件的背景色會變暗一階，放開後恢復原狀，這是最通用、拿來取代一般按鈕或連結的首選。`TouchableOpacity` 的做法則是透過「降低透明度」來提供回饋，按下去的時候整個元件會變得比較透明、隱約可以看到底下的背景，放開後恢復不透明，這種效果比較柔和，是實務上非常常見的選擇。",
          "如果你的目標平台鎖定在 Android，還可以考慮用 `TouchableNativeFeedback`，它會呈現出 Android 系統原生特有的「墨水擴散」（ripple）觸控回饋效果，也就是點下去的位置會出現一圈類似水波紋往外擴散的視覺動畫，這個效果是 Android Material Design 介面規範裡很標誌性的觸控回饋方式，用這個元件可以讓你的按鈕在 Android 上感覺更「原生」、更貼近該平台使用者習慣的操作手感。",
          "最後一種是 `TouchableWithoutFeedback`，顧名思義，它只負責偵測使用者的點按手勢、觸發你指定的函式，但完全不會提供任何視覺上的按壓回饋效果。這個元件適合用在「這個區域本身已經有其他方式呈現互動感（例如底下的內容本身會變化），你不需要額外再疊加一層變暗或變透明的效果」的情境，算是四個 `Touchable` 元件裡使用頻率相對較低、但在特定場合仍然有用的選項。另外，這四個 `Touchable` 元件都共通支援一個 `onLongPress` 屬性，可以用來偵測「使用者按住不放超過一段時間」的長按手勢，用法上直接傳一個函式進去就能監聽，不需要額外的手勢邏輯。",
          "值得一提的是，React Native 目前實務上更推薦優先使用的元件其實是 `Pressable`，它可以視為 `Touchable` 系列元件的現代化整合版本，設計目的就是要用一個更彈性、更完整的 API 去取代原本分散在 `TouchableHighlight`、`TouchableOpacity`、`TouchableWithoutFeedback` 之間的功能。`Pressable` 可以細緻地偵測觸控的每一個階段：`onPressIn` 在手指剛碰到畫面的當下就會觸發、`onPressOut` 在手指放開的瞬間觸發、`onPress` 則是在 `onPressOut` 之後才呼叫，此外一樣支援 `onLongPress` 偵測長按（預設超過 500 毫秒觸發，這個時間也可以透過 `delayLongPress` 屬性調整）。",
          "`Pressable` 真正厲害的地方在於它的 `style` 屬性可以直接傳一個函式進去，這個函式會收到目前的按壓狀態（一個包含 `pressed` 布林值的物件）當作參數，讓你可以直接寫出「按下去的時候要長怎樣、放開的時候要長怎樣」的樣式邏輯，例如 `style={({ pressed }) => ({ opacity: pressed ? 0.5 : 1 })}` 就能做出跟 `TouchableOpacity` 幾乎一樣的按壓變透明效果，但寫法更直觀、也更容易再疊加其他自訂樣式判斷。除此之外，`Pressable` 還支援 `hitSlop` 屬性，可以在元件視覺範圍之外「額外擴增」一圈可以被點到的判定區域，這對於畫面上一些視覺尺寸做得比較小、但又希望使用者好點擊的按鈕特別實用；在 Android 上也支援 `android_ripple` 屬性，可以設定要不要顯示、以及顯示成什麼樣式的原生水波紋效果。",
          "至於「滑動」跟「捲動」這類手勢，例如捲動一長串清單、或是左右滑動切換頁面，這些不是 `Button` 或 `Touchable`／`Pressable` 系列元件負責的範疇，而是要交給 `ScrollView` 這個核心元件來處理，這部分屬於另一個獨立的主題，這裡先知道「觸控點按用 `Touchable`／`Pressable`，捲動滑動用 `ScrollView`」這個大方向的分工就好。另外官方文件也誠實列出了一個目前存在的已知限制：不管用哪一種觸控元件，它們可以感應到的觸控範圍都不會超出父層 `View` 的邊界，而且在 Android 平台上，也不支援用負的 `margin` 數值去讓觸控範圍反而擴張超出視覺邊界，這是實作上如果遇到「明明點在按鈕旁邊一點點卻沒反應」時，可以先想到的已知原因之一。",
        ],
      },
      {
        id: "rn-9",
        title: "使用 ScrollView",
        paragraphs: [
          "在 React Native 裡，如果畫面上的內容比手機螢幕還要長，第一個會想到的元件通常就是 `ScrollView`。它是一個很通用的捲動容器，可以把各式各樣、內容完全不同的子元件通通塞進去，例如一段文字接著一張圖片、再接著一個按鈕，這些混雜的內容都可以一起放進同一個 `ScrollView` 裡讓使用者上下捲動瀏覽，不需要每種內容類型分別處理。",
          "最簡單的用法就是把 `ScrollView` 當成一個外層容器，裡面直接放你想要垂直排列、可以捲動的內容，比如說同時放好幾張圖片跟好幾段說明文字，混合排列也完全沒問題。這種彈性正是 `ScrollView` 最大的賣點：不像有些清單元件要求資料格式統一，`ScrollView` 什麼都能收，寫法也跟一般排版元件差不多直覺。",
          "`ScrollView` 預設是垂直捲動，但只要加上 `horizontal` 這個屬性設成 `true`，整個捲動方向就會變成水平，很適合拿來做像是圖片輪播、標籤列表這種左右滑動的畫面。這個屬性一開就是整個容器的捲動軸向整個翻轉，不需要額外調整內部排版邏輯。",
          "如果你想做出「一頁一頁翻」的效果，像是新手導覽頁那種滑一下就整頁切換、不會停在頁面中間的效果，可以打開 `pagingEnabled` 屬性，讓使用者的滑動手勢自動吸附到下一個整頁的位置。不過官方文件也提到，如果是要在 Android 上做水平方向的翻頁切換，也可以考慮用社群的 `ViewPager` 元件來實作，效果可能更貼近原生的分頁體驗。",
          "在 iOS 上，`ScrollView` 還內建了一個蠻好玩的功能：如果裡面只放一個子項目，你可以透過設定 `maximumZoomScale` 跟 `minimumZoomScale` 這兩個屬性，讓使用者可以用雙指去捏合或撐開來縮放內容，很適合拿來做圖片檢視器這種需要放大細節的畫面。",
          "不過 `ScrollView` 有一個非常重要、初學者一定要知道的限制：它會把裡面所有的子元件一次全部渲染出來，不管這些內容目前有沒有真的顯示在螢幕範圍內。也就是說，就算你的清單有一百筆資料，`ScrollView` 還是會把這一百筆全部畫出來放在記憶體裡，只是使用者暫時看不到而已。",
          "這個「全部渲染」的特性，在內容量不大的時候完全沒問題，甚至可以說是優點，因為所有東西一開始就準備好了，捲動起來很順暢。但只要資料筆數一多，或是裡面放了很多張圖片這種比較吃記憶體的內容，效能就會開始變差，App 可能會在剛進入畫面的時候明顯卡頓，甚至在低階手機上直接吃緊記憶體。",
          "所以官方的建議很明確：`ScrollView` 比較適合用在內容量少、大小可預期的畫面，例如一個設定頁面、一篇文章內文這種情境。如果你要顯示的是一份可能有幾十筆甚至上百筆、而且資料可能還會不斷變動的清單，就不應該用 `ScrollView`，而是要改用下一節會介紹的 `FlatList`，它只會渲染畫面上看得到的部分，效能表現好上很多。",
        ],
      },
      {
        id: "rn-10",
        title: "使用清單視圖（FlatList／SectionList）",
        paragraphs: [
          "上一節提到 `ScrollView` 會把所有子元件一口氣全部渲染出來，這在資料量大的時候會拖垮效能，這也是為什麼 React Native 特別另外準備了一組專門處理清單的元件，其中最常用的就是 `FlatList`。它是專門設計來顯示「結構相似、但內容可能會一直變動」的資料，例如一份聯絡人清單、一串商品列表，或是從伺服器一頁一頁載入的貼文。",
          "`FlatList` 跟 `ScrollView` 最核心的差異，就在於「虛擬化渲染」這個機制。簡單來說，`FlatList` 並不會把資料裡的每一筆都真的畫成畫面上的元件，而是只渲染目前螢幕看得到、以及即將捲動進來的那一小段範圍，使用者往下捲動的時候，畫面外面的舊項目才會被回收、新項目才會被建立出來遞補。這讓 `FlatList` 即使面對成千上百筆資料，也能維持流暢的捲動效能，記憶體用量也不會隨資料量線性暴增。",
          "要讓 `FlatList` 動起來，有三個屬性幾乎是必備的，第一個是 `data`，這就是你要顯示的原始資料陣列，可以是任何格式的物件陣列，`FlatList` 會依序把裡面每一筆資料拿出來處理。",
          "第二個必要屬性是 `renderItem`，這是一個函式，`FlatList` 會針對 `data` 陣列裡的每一筆資料呼叫一次這個函式，你在裡面決定「這一筆資料要長成什麼樣子」，通常會回傳一個包著 `Text` 或其他排版元件的畫面片段。舉例來說，如果 `data` 是一串字串陣列，`renderItem` 裡面就可以簡單地把每個字串包進一個 `Text` 元件顯示出來。",
          "第三個常常被新手忽略、但其實同樣重要的屬性是 `keyExtractor`，它的作用是告訴 `FlatList` 怎麼從每一筆資料裡取得一個獨一無二的識別字串，讓 `FlatList` 內部在重新渲染、資料更新的時候，可以正確地知道哪些項目是同一筆、哪些是新增或刪除的，避免畫面出現閃爍或渲染錯亂的問題。如果不特別指定，`FlatList` 預設會嘗試使用資料裡的 `key` 或 `id` 欄位，找不到才會退而求其次用陣列索引，但索引在資料順序會變動時容易出問題，所以官方建議還是自己明確提供 `keyExtractor`。",
          "當你的清單資料需要「分組顯示」的時候，例如通訊錄依照姓氏字母分組、設定頁面依照類別分區塊，這時候比 `FlatList` 更適合的元件是 `SectionList`。它可以讓你把資料切成好幾個區段，每個區段除了自己的清單項目之外，還可以有一個獨立的區段標題，概念上很類似 iOS 原生 `UITableView` 裡常見的分組清單樣式。",
          "`SectionList` 跟 `FlatList` 用起來的邏輯很接近，一樣需要提供渲染每一筆項目的函式跟識別用的 `key`，只是資料結構多了一層「分組」，你需要把原始資料整理成一個個帶有標題跟該組項目陣列的區塊，再交給 `SectionList` 去統一處理捲動跟虛擬化渲染，不需要自己手動拼接標題跟內容的排版邏輯。",
          "在實際開發 App 的時候，清單元件最常見的應用場景就是顯示從伺服器抓回來的資料，比如打 API 拿到一批文章或商品資料後，直接餵給 `FlatList` 的 `data` 屬性顯示出來。這也是為什麼幾乎每個稍具規模的 App，只要牽涉到「顯示一串會變動的資料」，最後都會用到 `FlatList` 或 `SectionList`，而不是簡單的 `ScrollView`。",
        ],
      },
      {
        id: "rn-11",
        title: "圖片",
        paragraphs: [
          "在 React Native 裡處理圖片，跟寫網頁時的習慣不太一樣，最大的差異在於元件名稱本身：不是用 `img` 標籤，而是用 `Image` 元件，而且指定圖片來源用的屬性也不是 `src`，而是 `source`，這個小小的命名差異，是初學者剛開始很容易寫錯的地方。",
          "如果是你自己放在專案裡的靜態圖片，例如一個圖示或按鈕圖片，寫法會是 `Image` 搭配 `source` 屬性，裡面用 `require` 把圖片檔案路徑引入進來，就像 `require('./my-icon.png')` 這樣。這裡有個重要限制：`require` 裡面的路徑字串必須在寫程式碼的當下就能靜態確定，不能用變數拼接字串的方式動態組出檔名，如果真的需要依條件切換圖片，正確做法是把整個 `require(...)` 包在條件判斷式裡，分別對應到不同的圖片檔案。",
          "靜態圖片還有一個很方便的地方，就是它的寬高資訊會被打包工具自動讀取進來，所以大多時候你不需要自己額外指定 `width` 跟 `height`，React Native 在圖片載入時就已經知道原始尺寸了。",
          "但如果圖片是從網路上動態載入的，例如使用者的大頭貼、伺服器回傳的商品圖，寫法會變成 `source` 裡面放一個物件，用 `uri` 屬性指定圖片網址，例如 `source={{uri: 'https://example.com/photo.png'}}`。這種情況下，React Native 沒辦法在畫面渲染之前預先知道這張圖片實際的尺寸大小，所以你一定要自己手動在 `style` 裡明確指定 `width` 跟 `height`，不然這張圖片預設會完全不顯示、佔用的空間是零。",
          "這個「一定要手動給尺寸」的規則乍聽有點麻煩，但其實是刻意的設計。想像一下在網頁上常遇到的狀況：圖片還沒下載完之前，瀏覽器常常會先用零尺寸占位，等圖片下載好才突然把版面撐開，導致畫面上的文字、按鈕位置忽然跳動，這種體驗很不好。React Native 選擇讓開發者提前就講清楚圖片要佔多大空間，這樣不管圖片下載得快或慢，版面的排版空間都是穩定的，不會有這種畫面跳動的問題。",
          "圖片放進固定尺寸的框框裡之後，接下來要處理的問題是：如果圖片的原始長寬比例跟你設定的框框比例對不上，圖片要怎麼填滿或裁切，這時候就要靠 `resizeMode` 這個屬性。常見的幾種模式包括 `cover`（把圖片等比例放大到填滿整個框框，超出的部分裁掉，是最常拿來做背景圖或封面圖的做法）、`contain`（把整張圖片完整塞進框框裡，維持原始比例，可能會有留白）、以及 `stretch`（不管原始比例、直接把圖片硬拉伸填滿整個框框，可能會讓圖片變形）。開發時可以依照畫面設計實際需求去試這幾種效果，挑出視覺上最合適的一種。",
          "另外一個常讓新手困惑的地方，是為什麼同一張圖片在專案資料夾裡常常會看到好幾個版本，檔名分別多了 `@2x`、`@3x` 這樣的後綴。這其實是在因應手機螢幕解析度不同的問題：現在的手機螢幕密度差異很大，同一張圖片如果只準備一種解析度，在高密度螢幕的手機上顯示出來就會模糊。所以 React Native 支援你同時準備同一張圖的多種解析度版本，例如 `check.png`、`check@2x.png`、`check@3x.png`，打包工具會依照使用者手機的實際螢幕密度，自動挑選最接近、最合適的那一個版本載入，開發者完全不需要自己寫判斷邏輯去切換，只要照這個命名規則把檔案放好即可。",
          "最後值得一提的是，React Native 對圖片的解碼是放在背景執行緒處理的，不會佔用主執行緒的效能去阻塞畫面的其他互動，這代表就算圖片比較大、解碼要花一點時間，畫面上其他按鈕、捲動之類的操作也不會因此卡住，使用者頂多是多看幾格圖片還沒完全顯示出來的過渡畫面而已，體感上會比較流暢自然。",
        ],
      },
      {
        id: "rn-12",
        title: "平台差異程式碼（iOS／Android）",
        paragraphs: [
          "React Native 最初吸引人的地方，就是「一套程式碼、兩個平台都能跑」，同一份 JavaScript 邏輯跟畫面元件，理論上可以同時產出 iOS 版跟 Android 版的 App。但實際開發久了就會發現，iOS 跟 Android 畢竟是兩個設計哲學不完全相同的作業系統，有些畫面互動的細節、甚至某些原生功能的呼叫方式，還是需要針對不同平台寫出不太一樣的處理邏輯，這時候就需要「平台差異程式碼」的概念。",
          "最基礎、也是最常用的做法是透過 `Platform` 這個模組，裡面有一個 `Platform.OS` 屬性，當程式在 iOS 裝置上執行的時候它的值會是 `'ios'`，在 Android 裝置上則會是 `'android'`。你可以簡單用一個條件判斷式，依照這個值決定某個樣式或某段邏輯要怎麼寫，例如某個區塊在 iOS 上的高度要設 200，在 Android 上則設成 100，只要一行三元運算式就能處理完。這種寫法特別適合「只有小小一處」需要因平台而異的情境。",
          "如果需要依平台切換的設定不只一兩個值，而是同時牽涉到好幾個屬性，`Platform` 模組還提供一個更好用的 `Platform.select` 方法。它接受一個物件，裡面用 `ios`、`android`、`native`、`default` 這幾個 `key` 分別對應不同情境的設定值，React Native 執行的時候就會自動挑出符合目前平台的那一組回傳出來。比如說同一個容器樣式，可以透過 `Platform.select` 讓背景色在 iOS 上是紅色、在 Android 上是綠色，其他平台（例如網頁版）則統一用藍色，寫起來比堆疊一堆三元運算式清楚很多。",
          "值得注意的是這幾個 `key` 之間有優先順序：如果程式在手機上執行，會優先找 `ios` 或 `android` 這兩個對應平台的設定；如果沒有寫這兩個 `key`，才會退而求其次去找 `native` 這個 `key`；如果連 `native` 都沒有，最後才會落到 `default` 這個保底設定，通常用來涵蓋像網頁版這種其他執行環境。",
          "`Platform.select` 不只可以用來決定樣式數值，其實它可以回傳任何型別的東西，包括整個元件。也就是說你可以寫出「這個元件在 iOS 上載入某一份實作、在 Android 上載入另一份實作」這種依平台切換整個元件實作的寫法，讓比較複雜的原生功能可以分別用最適合各自平台的方式去寫。",
          "除了判斷是 iOS 還是 Android 之外，`Platform` 模組也能拿來偵測比較細緻的系統版本號。在 Android 上，`Platform.Version` 拿到的其實是 Android 的 API Level 這個編號系統，不是我們平常講的 Android 幾點幾版本，需要對照官方版本歷史表才知道對應到實際的系統版本；在 iOS 上，`Platform.Version` 拿到的則是一串像 `'10.3'` 這樣的字串，代表目前系統版本，如果只需要判斷主要版本號，可以用 `parseInt` 轉成數字再比較。",
          "當某個畫面或功能在兩個平台上的差異已經大到「幾乎是整段邏輯都要分開寫」，而不只是一兩個屬性值不同的時候，寫一堆 `Platform.OS` 的條件判斷式就會讓程式碼變得又長又難維護。這時候 React Native 提供了另一種更乾淨的做法：直接把檔名拆成兩份，一份叫做某某元件`.ios.js`，另一份叫做某某元件`.android.js`，兩份檔案各自寫各自平台的完整實作。",
          "這個檔名機制最方便的地方，在於你在別的地方要使用這個元件時，完全不用管背後其實有兩份不同的實作，只要照平常的方式 `import` 這個元件的基本檔名（不含 `.ios` 或 `.android` 後綴）就好，React Native 的打包工具會自動根據目前建置的平台，去挑選對應副檔名的那一份檔案編譯進去，另一份完全不會被打包進最終的 App 裡，也不會有任何額外的判斷邏輯散落在呼叫端的程式碼裡。",
          "除了 `.ios.` 跟 `.android.` 這兩種副檔名，React Native 還支援一種 `.native.js` 的副檔名，這是設計給那些同時要在網頁版（例如用 React 寫的 Web 版本）跟手機版之間共用程式碼的專案用的：同一個元件可以有一份給 Web 打包工具（像 `webpack`）讀取的一般檔案，再搭配一份 `.native.js` 讓 `Metro`（React Native 的打包工具）讀取，這樣不管是 iOS 還是 Android，都會共用同一份 native 版實作，跟純網頁版的實作分開維護。",
          "整體來說，判斷該用哪種方式處理平台差異其實有一個簡單的準則：差異很小、只是某個樣式數值或某一行邏輯不同，用 `Platform.OS` 或 `Platform.select` 就綽綽有餘；但如果差異大到整個元件的結構、互動邏輯都完全不一樣，那就該考慮乾脆拆成 `.ios.js` 跟 `.android.js` 兩份檔案，讓程式碼各自獨立、也更容易分別維護跟除錯，不用擠在同一份檔案裡用一堆條件式互相牽扯。",
        ],
      },
      {
        id: "rn-13",
        title: "網路請求",
        paragraphs: [
          "幾乎每一個像樣的 App 都需要跟遠端伺服器交換資料，可能是登入驗證、抓取列表、上傳圖片，這些都算是「網路請求」。React Native 在這件事情上其實對你很友善，因為它直接內建了瀏覽器世界你早就用過的 `fetch` API，不需要額外裝套件就能發請求。這代表如果你以前寫過網頁前端、用 `fetch` 打過 API，這裡的知識幾乎可以直接搬過來用，學習成本非常低，這也是官方文件把它列為第一個介紹方式的原因。",
          "最簡單的用法就是把網址丟給 `fetch`，例如 `fetch('https://mywebsite.com/mydata.json')`，它就會去對這個網址發出一個 `GET` 請求。但實務上你常常需要做更細緻的控制，例如發 `POST` 請求、帶自訂的標頭（`header`）、或是夾帶請求內容（`body`）。這時候 `fetch` 可以接收第二個參數，是一個設定物件，裡面可以指定 `method` 寫 `POST`，`headers` 裡放 `Accept` 跟 `Content-Type` 告訴伺服器你要傳送和接收 JSON 格式，`body` 則放上你要送出的資料，通常會用 `JSON.stringify` 把 JavaScript 物件轉成字串再送出去。",
          "網路請求天生就是非同步的，因為你不知道伺服器什麼時候會回應，甚至不知道會不會回應。`fetch` 回傳的是一個 `Promise`，最傳統的寫法是用 `.then()` 串接：先 `.then(response => response.json())` 把回應內容解析成 JSON，再用下一個 `.then()` 拿到真正的資料去使用，最後用 `.catch()` 接住任何可能發生的錯誤。這種串鏈式寫法一開始看起來有點繞，但概念就是「等這一步做完，再做下一步」。",
          "如果你覺得一串 `.then()` 看起來太囉唆，React Native 也完全支援 `async/await` 這種更接近同步寫法的語法糖，讓非同步程式碼讀起來像一般由上到下執行的程式碼。你可以把函式標記成 `async`，裡面用 `await` 等待 `fetch` 完成、再 `await` 等待 `response.json()` 解析完成，搭配 `try/catch` 包起來處理錯誤。這種寫法在真實專案裡幾乎已經是主流，因為邏輯清楚、少了一層層的縮排巢狀。",
          "這裡有一個非常容易被新手忽略、但官方文件特別強調的重點：一定要用 `try/catch`（或 `.catch()`）把 `fetch` 可能丟出的錯誤接住，否則錯誤會被「無聲無息」地吞掉，你的 App 可能只是安靜地沒有反應，卻完全不知道背後其實網路請求失敗了，例如網路斷線、伺服器掛掉、或是 JSON 格式不對。養成每次寫 `fetch` 都主動處理錯誤的習慣，能省下大量除錯的時間。",
          "如果你之後真的要打包成 iOS 或 Android 的正式 App，有兩個平台特有的安全機制要知道。iOS 從 9.0 起預設會強制啟用一個叫 App Transport Security（ATS）的機制，要求所有連線都用 HTTPS 加密，如果你的 API 網址還是用不加密的 http 開頭，預設會直接被擋下來，需要在專案設定裡額外加白名單例外，蘋果審核上架時也會要求你說明為什麼需要放行明文連線。",
          "Android 這邊從 API Level 28 開始也有類似的限制，預設一樣會擋掉明文（不加密）的網路流量，如果真的有需要，可以在應用程式的設定檔裡加上 `android:usesCleartextTraffic` 這個屬性來覆蓋這個限制。這兩個平台的機制看起來麻煩，但用意都是希望你的 App 預設就走安全連線，減少資料被竊聽或竄改的風險，是現代行動開發很基本的安全常識。",
          "除了 `fetch`，React Native 其實也內建了 `XMLHttpRequest` 這個更早期的網路 API，這代表很多依賴它的第三方套件（例如 `axios`）在 React Native 裡也能正常運作，你不需要 `fetch` 不會的東西它就一定沒有。不過要特別提醒一件事：React Native 裡的 `XMLHttpRequest` 安全模型跟瀏覽器不一樣，因為原生 App 並沒有網頁世界那個「CORS 跨網域限制」的概念，所以行為上會跟你熟悉的瀏覽器版本有些出入，遇到怪異狀況時可以先往這個方向想。",
          "如果你的需求是「即時雙向溝通」而不是一來一回的請求，例如聊天室、即時通知、股票報價這種需要伺服器主動推資料給你的場景，這時候該用的就不是 `fetch` 而是 WebSocket。用法是 `new WebSocket` 建立連線後，透過 `ws.onopen` 監聽連線成功、`ws.onmessage` 監聽收到訊息、`ws.onerror` 監聽錯誤、`ws.onclose` 監聽連線關閉，要送訊息出去就呼叫 `ws.send()`。整個概念是先建立一條長期保持開啟的通道，雙方可以隨時互相傳訊息，跟 `fetch` 那種「問一次答一次就結束」的模式很不一樣。",
          "最後提醒一下，`fetch` 目前還是有一些已知的小限制，例如不支援 `redirect: manual` 這種手動控制重新導向的選項，跟 Cookie 有關的登入驗證在某些情況下（尤其是 iOS 遇到 302 轉址時）行為不太穩定，開發時如果發現登入狀態怪怪的，可以往這個方向去查官方 GitHub 上記錄的已知問題。這些屬於比較進階、遇到才需要處理的細節，剛開始學習不用太擔心，先把基本的 `fetch` 搭配 `async/await` 用熟，就已經足夠應付大多數的資料串接需求了。",
        ],
      },
      {
        id: "rn-14",
        title: "畫面間導航",
        paragraphs: [
          "手機 App 幾乎不可能只有一個畫面，你一定會有首頁、詳細頁、設定頁，使用者要能在這些畫面之間切換、上一頁下一頁地穿梭。但這裡有一件事情新手常常會誤會：React Native 這個框架本身其實並沒有內建「畫面導航」這個功能，它只負責幫你把畫面畫出來，至於「怎麼從 A 畫面跳到 B 畫面」這件事，官方是刻意把它交給社群方案去解決的，而不是像某些框架一樣寫死一套。",
          "官方文件推薦的標準做法是使用一個叫 React Navigation 的獨立函式庫，這是目前 React Native 生態圈裡最主流、最多人用的導航方案，幾乎所有教學和範例專案預設都是用它。除此之外還有另一條路叫 `react-native-navigation`，這個方案是用原生程式碼實作導航邏輯，比較適合「已經有一套原生導航架構、要把 React Native 嵌進去」的情境，對大多數從零開始的新手來說，React Navigation 才是該學的那一個。",
          "如果你是全新專案，官方甚至準備了一行指令幫你直接生成一個內建 React Navigation 的範例：`npx create-expo-app@latest --template react-navigation/template`，這個範本已經幫你把常見的設定都串好，對第一次接觸導航概念的人是很好的起手式。如果是要加進既有專案，則需要自己手動安裝套件：先裝 `@react-navigation/native` 這個核心函式庫，再裝 `@react-navigation/native-stack` 這個「堆疊式」導航器的實作。",
          "裝完核心套件後還有幾個所謂的 peer dependency 要補上，例如 `react-native-screens`（讓畫面切換效能更好、更接近原生）跟 `react-native-safe-area-context`（處理手機瀏海、底部導航列這些「安全區域」的版面問題）。如果是純 React Native（不是用 Expo）的專案，iOS 這邊裝完套件後還要多跑一次 `cd ios` 再 `pod install`，把原生層的相依套件也裝好，這是因為 iOS 的套件管理系統跟 JavaScript 這邊是分開的。",
          "接下來要理解的核心概念是「導航器」（navigator）。你可以把它想成是一個負責管理一堆畫面、決定畫面怎麼疊放、怎麼切換的容器。最常見的一種叫 Stack Navigator（堆疊導航器），概念很像疊盤子：你進到新畫面就是往上疊一層，按返回鍵就是把最上面那層拿掉，回到原本那一層。程式碼上你會用 `createNativeStackNavigator` 建立一個這樣的導航器，裡面用 `screens` 屬性列出所有畫面，每個畫面指定要顯示哪個元件（`screen`），也可以額外設定 `options`（例如畫面標題）。",
          "設定好整個導航結構之後，要用 `createStaticNavigation` 把這份設定轉換成一個真正可以放進 App 裡渲染的元件，你的 App 進入點通常就只是簡單地渲染這個 Navigation 元件就好。要從一個畫面跳到另一個畫面，你會在元件裡用 `useNavigation` 這個 hook 拿到導航物件，然後呼叫 `navigation.navigate('目標畫面名稱')` 就能觸發切換，這是整個 React Navigation 用起來最頻繁的一行程式碼。",
          "很多時候你跳轉畫面時還想順便帶一點資料過去，例如點了某個使用者的名字要跳到他的個人頁面，並且要讓那個頁面知道「是哪個使用者」。這時候 `navigate` 可以接收第二個參數當作參數，例如 `navigation.navigate('Profile', {name: 'Jane'})`，而在目標畫面裡，則是透過元件收到的 `route` 屬性、用 `route.params.name` 把這個資料讀出來，這一套「傳參數、收參數」的機制是畫面間溝通最基本的方式。",
          "值得一提的是，Native Stack Navigator 這個東西在底層其實不是純 JavaScript 模擬出來的假動畫，而是真的去呼叫 iOS 原生的 `UINavigationController`、Android 原生的 `Fragment` 機制來做畫面切換，所以你感受到的滑動轉場、返回手勢，都是道地的原生體驗，而不是網頁那種用 CSS 做出來的仿真動畫，這也是它的效能和手感能跟純原生 App 打平的原因。",
          "除了「一層疊一層」的 Stack 導航之外，React Navigation 也提供其他常見的導航模式，例如畫面底部常見的分頁切換 Tab Navigator，或是從螢幕邊緣滑出來的側邊選單 Drawer Navigator，實務上一個 App 常常是好幾種導航器組合搭配使用，例如外層是 Tab、其中一個 Tab 裡面又包一個 Stack。這篇文件本身只帶到入門觀念，如果要深入每一種導航器的細節設定，官方會建議你接著去讀 React Navigation 自己的完整教學文件。",
          "最後想強調一個跟網頁開發思維上很不一樣的地方：網頁的導航是靠網址（URL）驅動的，每個頁面對應一個路徑，使用者可以直接輸入網址跳過去，瀏覽器的上一頁下一頁按鈕也是靠瀏覽紀錄運作。但手機 App 沒有網址列這種東西，畫面之間的關係是靠程式碼裡明確寫出來的「畫面堆疊」在管理，你必須自己定義好哪些畫面存在、怎麼跳轉、參數怎麼傳，這也是為什麼 React Native 需要一個獨立的導航函式庫來補上這塊，而不是像網頁框架那樣把路由當作語言本身的一部分。",
        ],
      },
      {
        id: "rn-15",
        title: "除錯基礎",
        paragraphs: [
          "寫程式難免會出錯，React Native 也幫開發者準備了一整套除錯工具，讓你在開發階段能快速找到問題出在哪裡。這裡要先提醒一個很重要的觀念：這一整套除錯功能，包含等一下要介紹的開發者選單、`LogBox` 錯誤提示、還有 React Native DevTools，全部都只在開發模式下才會出現，一旦你把 App 打包成正式發佈版本，這些工具會自動被關閉，使用者手上的正式版 App 不會看到任何這些除錯畫面。",
          "第一個要認識的工具是「開發者選單」（Dev Menu），這是一個內建在 App 裡、專門給開發者用的隱藏選單。要叫出它，在實機或模擬器上通常是「搖一搖手機」，如果你是在電腦上用模擬器測試，也有對應的鍵盤快捷鍵：iOS 模擬器是按 Ctrl + Cmd + Z（或是從選單列選 Device > Shake），Android 模擬器則是 Mac 上按 Cmd + M、Windows 或 Linux 上按 Ctrl + M，如果是實體 Android 裝置接電腦調試，也可以下指令 `adb shell input keyevent 82` 來叫出選單。",
          "打開這套除錯功能後，最核心的一項就是 React Native DevTools，這是 React Native 內建的除錯器，概念很像你在網頁瀏覽器裡按 F12 打開的開發者工具，可以讓你檢查、理解你的 JavaScript 程式碼實際上是怎麼運作的。要打開它有兩種方式：一種是從剛剛提到的開發者選單裡選「Open DevTools」，另一種是如果你是用命令列（CLI）在跑開發伺服器，直接在終端機按下 j 這個按鍵就可以叫出來，不需要額外操作手機或模擬器。",
          "第一次打開 React Native DevTools 時，它會先顯示一個歡迎面板，同時打開一個「主控台」（console）抽屜，讓你可以在裡面看到程式裡 `console.log` 印出來的訊息，也能直接在裡面跟正在執行的 JavaScript 環境互動、下指令測試。從視窗上方，你還可以切換到其他更進階的面板，例如可以直接檢查 React 元件樹狀結構的「元件檢查器」，以及用來分析效能瓶頸的「Profiler」效能分析器，這些對後期要優化 App 效能會很有幫助。",
          "另一個新手一定會很快遇到的工具叫 `LogBox`，顧名思義就是一個專門顯示警告和錯誤的日誌框，只要你的程式碼記錄了警告（warning）或錯誤（error），它就會自動跳出來蓋在 App 畫面上提醒你。如果遇到的是「致命錯誤」，例如你的程式碼有語法錯誤導致整個 JavaScript 沒辦法執行，`LogBox` 會直接鎖住畫面、顯示錯誤發生的確切位置，而且這個畫面在你修好程式碼之前是關不掉的——因為程式碼根本跑不起來，只有等你把語法修正、透過 Fast Refresh 或手動重新整理之後，它才會自動消失。",
          "如果只是一般等級的警告或錯誤（程式還能繼續跑，只是有些地方寫得不太對），`LogBox` 會用比較溫和的方式呈現：錯誤會用紅色徽章顯示訊息數量，點下去可以看展開的詳細內容、還能一則一則往後翻；警告則是用黃色橫幅提示，但不會馬上展開細節，而是建議你去 DevTools 裡查看完整內容。值得注意的是，一旦你打開了 React Native DevTools，除了致命錯誤之外的其他錯誤訊息就會改成藏在 DevTools 的主控台面板裡，官方也建議把 DevTools 主控台當成「唯一真相來源」，因為 `LogBox` 本身有各種選項可以把某些日誌隱藏或降級，不一定會顯示全部訊息。",
          "如果有些警告訊息實在太吵、或是根本沒辦法修（例如來自你安裝的第三方套件、不是你自己寫的程式碼），React Native 也提供了 `LogBox` 這個 API 讓你自己調整顯示規則。從 `react-native` 這個套件裡引入 `LogBox` 之後，呼叫 `LogBox.ignoreAllLogs()` 可以整個關掉所有日誌通知，這在你要對外展示、做產品 demo 時特別實用，不希望螢幕上突然跳出一堆警告嚇到客戶。",
          "如果你只是想針對「特定幾則」訊息消音，而不是整組全關，可以用 `LogBox.ignoreLogs()` 搭配一個陣列，裡面可以放完全符合的文字字串，也可以放正規表達式做模糊比對，例如篩掉所有符合某個關鍵字規則的第三方套件警告訊息。這種細緻控制讓你可以保留真正重要的警告，只把已知、無害、暫時修不了的雜訊過濾掉，是實務開發中常會用到的小技巧。",
          "除了看程式碼邏輯上的錯誤，行動裝置開發還有一塊很重要的除錯面向叫「效能」。開發者選單裡有一個叫 Perf Monitor（效能監視器）的選項，打開之後畫面上會疊一層半透明的效能數據，顯示畫面更新的頻率等資訊，讓你在開發過程中大致抓一下這個畫面流不流暢。不過官方也老實提醒：這個內建的效能監視器只適合當作「粗略的參考指標」，真的要做精準的效能測量，還是建議搭配 Android Studio 或 Xcode 這些原生開發工具內建的效能分析器來看，數據會更準確可信。",
          "整體來說，React Native 的除錯流程可以想成一個由淺入深的路徑：先用搖一搖打開開發者選單熟悉環境、再用 j 鍵或選單打開 DevTools 看 console 訊息跟元件結構、遇到錯誤時先看 `LogBox` 跳出來的提示訊息判斷是致命錯誤還是一般警告、覺得吵就用 `LogBox` API 消音、最後如果要關心效能表現，再進一步用 Perf Monitor 或原生工具去量測。掌握這幾個工具的基本操作，遇到問題時就不會手忙腳亂，而是知道該打開哪個面板去找線索。",
        ],
      },
      {
        id: "rn-16",
        title: "更多資源與下一步",
        paragraphs: [
          "當你把 React Native 的核心概念（元件、樣式、互動、網路請求、導航、除錯）都摸過一輪之後，接下來的路其實很開放，沒有一條「標準答案」，而是看你想往哪個方向深入。官方文件建議可以往幾個面向補強：更熟悉開發環境的設定與在實機上跑起來的完整流程、把版面設計和 Flexbox 排版練得更熟練、深入研究前面提過的除錯工具、以及學習怎麼寫「跨平台程式碼」，也就是同一份程式碼要怎麼針對 iOS 和 Android 寫出不同邏輯或樣式的技巧。",
          "如果你想把基礎打得更紮實，官方也建議往兩個方向的官方文件回頭補課：一個是 React 本身的官方文件（React Native 的元件邏輯、Hook、狀態管理這些概念全部都是承襲自 React，把 React 的基本功練好對寫 RN 幫助非常大）；另一個是 MDN 上的 JavaScript 教學與參考手冊，因為 RN 終究是用 JavaScript（或 TypeScript）寫成的，語言本身的細節（例如 `Promise`、陣列方法、解構賦值）如果不熟，寫起 RN 也會綁手綁腳。如果你的 App 需要用到平台特有的原生功能，也可以視情況去翻 Android 或 iOS 官方的原生開發文件。",
          "工具方面，官方推薦用 VS Code 當作主力編輯器，搭配一個叫「VS Code React Native Tools」的擴充套件，可以讓你在編輯器裡直接除錯、看變數內容，而不用完全依賴前面介紹的手機端除錯工具，對日常開發的順手度會有明顯提升。",
          "接下來想繼續深入這個生態圈，官方點名了兩個值得認識的平台或工具。第一個是 Expo，它不只是一個套件，而是一整套圍繞著 React Native 打造出來的開發、建置、發佈工作流程，好處是你可以完全不用先裝 Xcode 或 Android Studio 就能開始寫 App、甚至直接雲端建置出可安裝的版本，對新手來說門檻低很多；如果之後真的需要用到某些原生功能，Expo 也支援你用 `npx expo prebuild` 產生原生專案、搭配 development build 跟 config plugin 逐步加進原生程式碼，不需要整個放棄 Expo 的工作流程。",
          "第二個是 Ignite，這是一套由社群維護的「starter kit」命令列工具，它會幫你生成一個已經幫你決定好一整套技術棧的 React Native 專案骨架，例如內建狀態管理方案 MobX-State-Tree、已經串好 React Navigation、還有畫面與資料模型的產生器、現成的元件庫和主題系統、甚至連測試都幫你設定好了。如果你不想每次開新專案都要重新決定「這次要用哪個狀態管理套件」，Ignite 這種預先幫你做好決定的方案可以省下不少摸索時間。",
          "想找靈感或看看別人怎麼做，官方也整理了實際案例可以參考：一個是 React Native 官方自己的展示台（Showcase），收錄了知名企業用 React Native 做出來的正式產品；另一個是社群維護的一份 GitHub 開源 App 清單，裡面每一個都附原始碼，可以直接抓下來在模擬器或實機上跑起來看，對照著程式碼學習別人的專案結構跟寫法，通常會比空想更有效率。",
          "最後，如果你之後有興趣往更進階、甚至想貢獻回生態圈的方向走，可以認識一下 React Native Directory 這個網站，這是社群維護的一份「非官方內建、由社群開發的元件與套件」搜尋目錄，想找某個功能的現成套件時很好用。如果你自己動手做出了某個原生元件或模組，官方文件也鼓勵你發佈到 NPM 和 GitHub 上跟社群分享，讓整個 React Native 生態圈可以一起變得更完整——這其實也是 React Native 之所以能有這麼多現成資源可以用的原因，每個人多回饋一點，後面的人就少走一點冤枉路。",
        ],
      },
    ],
  },
]
