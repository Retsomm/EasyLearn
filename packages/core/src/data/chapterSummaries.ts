import { chapters } from './chapters'

// 章節重點整理：給「複習」分頁用，內容改寫自各書章節，不是題目本身
// 目前只有 gfp（Grokking Functional Programming）驗證過，其餘章節之後陸續補上
export interface LevelSummary {
  levelId: string
  keyPoints: string[]
}

export const chapterSummaries: Record<string, LevelSummary[]> = {
  gfp: [
    {
      levelId: 'gfp-1',
      keyPoints: [
        '函式可以想成一個「黑盒子」：外部只看得到簽名（輸入型別、輸出型別），身體（實作邏輯）藏在盒子裡。FP 特別看重簽名要能完整交代這個函式在做什麼，讓使用者不用打開盒子看實作就能安心使用。',
        '「函式會說謊」：像 getFirstCharacter 拿到空字串會丟例外、divide 除以 0 會炸掉，這些函式的簽名（承諾回傳一個值）跟實際行為（可能整個中斷）對不上，這種落差正是程式最容易出錯的地方。',
        '命令式（imperative）著重「怎麼做」的每一步驟（例如用 for 迴圈逐字元累加分數）；宣告式（declarative）著重「要什麼」（例如直接用 word.length 描述分數即為字數）。書中把同一個計分需求分別用兩種風格實作，對照凸顯宣告式通常更精簡、更貼近需求描述。',
        '本書把函式式程式設計簡化定義成兩個核心特質：簽名不說謊、函式主體盡量宣告式——後面所有章節都是圍繞這兩點展開。',
      ],
    },
    {
      levelId: 'gfp-2',
      keyPoints: [
        '書中用購物車折扣範例示範不純函式的地雷：class 內部用 items 陣列和 bookAdded 布林值當狀態，addItem/removeItem 會偷偷改動這些狀態，一旦呼叫端直接操作 getItems() 回傳的陣列（例如直接呼叫 remove），或忘記同步更新 bookAdded，折扣計算就會跟真實購物車內容脫節、產生「幽靈折扣」。',
        '純函式的三條判斷準則：只回傳單一值、只根據參數計算結果（不偷看外部狀態）、不修改任何既有的值。書中逐步把 getDiscountPercentage 從讀取 bookAdded 狀態，改成每次都重新掃描 items 陣列即時計算（recalculating instead of storing），再把 items 直接當參數傳入，最終瘦身成一個不依賴任何內部狀態的靜態函式。',
        'referential transparency（參照透明）：如果一個函式呼叫可以整個替換成它的回傳值而不改變程式行為（例如 f(20) 永遠能被 19 取代），就代表這個函式是可信賴、可預測的——這是純函式最重要的實務好處之一。',
        'side effect（副作用）泛指函式除了計算回傳值以外做的任何事：發 HTTP 請求、印出訊息、寫資料庫、丟例外、甚至只是修改一個全域變數都算，純函式的定義就是完全不做這些事。',
        '純函式的測試特別直白：只要呼叫函式比對回傳值即可（例如 getDiscountPercentage([\'Book\']) === 5），不需要像不純版本那樣得先建立物件、呼叫好幾個方法設定內部狀態才能斷言——這也是為什麼純函式被視為更好維護、更好測試的基礎。',
      ],
    },
    {
      levelId: 'gfp-3',
      keyPoints: [
        '本章用歐洲旅行規劃的 replan(plan, newCity, beforeCity) 範例揭穿一個隱藏陷阱：即使函式回傳了正確的新清單 planB，只要它在實作裡直接對傳入的原始陣列做插入（mutate），呼叫端手上的 planA 也會被悄悄改掉——單看函式簽名完全看不出這個副作用，必須翻開實作才知道它不是純函式。',
        '修法是「複製再修改」：函式一開始就用傳入的陣列建立一份全新拷貝，所有插入/刪除都只作用在這份拷貝上，回傳這份拷貝，原始參數保持原封不動——重點是純函式可以自由修改自己在函式內部新建立的值，禁止的只是修改「從外部傳進來」的既有值。',
        '書中另一個賽車圈速範例（totalTime/avgTime 都對同一個 lapTimes 陣列呼叫 remove(0)）示範了共享可變狀態（shared mutable state）最陰險的一面：每個函式單獨測試都正確，兩個函式合在一起呼叫時卻因為互相修改到同一份陣列而得出錯誤的平均值，說明「狀態被多處共享並可修改」的風險會隨程式規模疊加，難以在小範圍測試中發現。',
        '解決共享可變狀態有三條路：程式碼複製後修改（前面用的手法）、物件導向的封裝（把可變狀態包在 class 內部、只透過方法存取，但要小心洩漏內部參照）、以及函式式的做法——乾脆換成不可變值，讓資料一旦建立就永遠無法被改變，任何「修改」都得產生一份新值。',
        '本章把函式式程式設計濃縮成一句定義：用純函式操作不可變值的程式設計方式——純函式是引擎，不可變值就是驅動這個引擎的燃料。',
      ],
    },
    {
      levelId: 'gfp-4',
      keyPoints: [
        '書中用單字排名需求（rankedWords）示範函式簽名如何從「說謊」一路修到「誠實」：一開始把排序邏輯（scoreComparator）寫在函式外部當隱藏依賴，函式簽名完全看不出它靠什麼排序；改成把比較邏輯／計分函式當作參數傳進來後，只看簽名就能知道這個函式的排序方式是可以替換的——這正是把函式當成值傳遞的核心價值。',
        'map、filter、foldLeft 都是「高階函數」（higher-order function：接受函式當參數，或回傳函式的函式）在 JS 裡的直接對應：map 對陣列每個元素套用同一個轉換函式產生等長新陣列；filter 用回傳布林值的判斷函式挑出符合條件的元素；foldLeft（對應 JS 的 reduce）則是帶著一個累加值，逐一走訪陣列元素把結果收斂成單一值，三者都不修改原陣列。',
        '函式可以回傳函式：當一個函式需要固定住某些參數（例如評分演算法、清單）、只留一個參數（例如門檻值）晚點才決定時，可以讓外層函式回傳一個只接收剩餘參數的內層函式，避免每次呼叫都要重複傳同一組固定參數。',
        'currying（柯里化）就是把一個多參數函式，轉換成一連串每次只接收一個參數、彼此回傳彼此的單參數函式鏈（在 JS 就是連續回傳箭頭函式，如 a => b => c => 結果）；重點不是語法炫技，而是讓呼叫端可以在程式的不同地方分別決定每個參數，減少重複傳遞相同引數的樣板碼。',
        'product type（乘積型別，Scala 用 case class 表達，對應到 JS 大致就是一個描述固定欄位形狀的物件，例如 { name, year }）用來把兩個以上相關聯的資料綁在一起變成一個不可變值；由於它本身不可變，跟 map／filter 這類高階函數搭配時特別安全，可以直接用箭頭函式取出欄位（例如 lang => lang.name）而不用擔心欄位被意外修改。',
      ],
    },
    {
      levelId: 'gfp-5',
      keyPoints: [
        '把一個陣列裡每個元素都轉成另一個陣列再攤平，會得到「陣列的陣列」；flatten 負責把這層巢狀攤平成一層，而 flatMap 就是「先 map 再 flatten」的合併版，而且輸出陣列的長度可以跟輸入不一樣（可以變短、變空、也可以變長），這點是 map 做不到的。',
        'flatMap 有一個特性：不管串接（chain）呼叫還是巢狀（nest）呼叫，只要每一步都回傳同一種容器型別，結果完全等價；也因為這個特性，才能安全地把一串 flatMap 改寫成巢狀寫法。書裡把這件事列為全書最重要的觀念之一。',
        '串接式的 flatMap 每一步只看得到「上一步」傳進來的值，前面步驟的中間變數會超出作用域；改成巢狀寫法可以讓所有前面算出來的值同時留在作用域內，這在最後一步需要同時用到好幾個前面步驟的值時（例如同時要用到書、作者、電影三個值）是必要的，但巢狀寫法會犧牲可讀性。',
        'for 迴圈式的語法糖（概念上類似 JS 裡把一串 promise 鏈改寫成 async/await 讓巢狀變好讀）在編譯期會被機械式地展開成巢狀的 flatMap／map 呼叫；當一次迭代多種集合型別（例如同時迭代陣列跟集合）時，最終回傳的容器型別是由「第一個」被迭代的集合型別決定，後面的都會被轉型去配合它。',
        '在這種鏈式寫法裡做篩選有三種手法：先對來源呼叫 filter、寫一個內嵌的條件判斷、或是寫一個函式在條件不成立時回傳空陣列、成立時回傳單一元素陣列——第三種手法比較通用，不只能用在陣列，任何「有 flatMap」的型別都適用，這替後面用同一套機制處理錯誤處理埋下伏筆。',
        '像 Option 這種「可能有值（Some）也可能沒值（None）」的容器一樣有 flatMap，所以同一套管線寫法可以直接拿來做資料驗證/解析：每個驗證步驟各自回傳 Option，串在一起後只要任何一步失敗（回傳 None），整個計算就會短路直接回傳 None，完全不用寫顯式的 null 檢查。',
      ],
    },
    {
      levelId: 'gfp-6',
      keyPoints: [
        '回傳 null 或丟例外會讓函式的型別簽名說謊（宣稱一定回傳某個值，實際上有時候什麼都給不出來甚至讓程式崩潰）；改用 Option 之後，「可能失敗」這件事本身就變成回傳型別的一部分，呼叫端沒辦法假裝看不到這個可能性。',
        '因為 Option 本身有 flatMap，所以多個各自驗證一個欄位的函式可以串成一條管線：只要其中一步回傳 None，整條計算立刻短路回傳 None，不用往下繼續執行——跟陣列 flatMap 的短路機制其實是同一套原理。',
        'checked exception 只是「讓你有機會處理」錯誤，卻不容易組合：想串接一個主要流程和一個備援流程，通常得手動巢狀 try-catch。Option／Either 提供了 orElse——一個單純接兩個「可能失敗的值」、回傳其中先成功那個的函式——組合起來乾淨很多，要疊多層備援策略只要一直串接 .orElse() 就好。',
        '處理一個「每個元素都可能各自失敗」的陣列時，書裡點名兩種策略：「盡力而為」（每個元素轉成 Option，最後只收集成功的，失敗的默默丟掉）跟「全有全無」（把整批 Option 摺疊成單一個 Option／Either，只要有一個失敗整批就算失敗）。',
        'Option 的弱點是失敗時只會給你一個 None，看不出到底哪裡出錯；Either（一個不是「成功值」就是「帶有說明文字的失敗值」的容器）解決了這個問題——你已經學過的 map／flatMap／orElse 這套語彙，原封不動可以用在 Either 上，只是失敗的那一邊多帶了一段描述訊息。',
        '把一條基於 Option 的驗證管線改寫成 Either，幾乎只是機械式替換：None 換成 Left("錯誤說明")、Some 換成 Right，管線的結構完全不用變——證明同一套模式一旦學會，換到另一個容器型別幾乎可以直接照搬。',
      ],
    },
    {
      levelId: 'gfp-7',
      keyPoints: [
        '只用字串、數字、布林這些原始型別建模業務資料，會讓編譯器接受各種不合理的組合（參數順序放反、一個布林旗標搭配一個只有旗標為真/假時才有意義的欄位、或是一個打錯字的「類型」字串）——型別系統完全幫不上忙，資料有效性只能全靠寫程式的人自己在每個用到資料的地方手動檢查。',
        '把某個字串包成一個「零成本包裝型別」（newtype，概念上類似 JS 裡用一個很輕量的 wrapper/branded type 來標記型別），可以避免不小心把兩個型別相同、意義不同的參數傳反（例如把地點誤填到類型的位置）——執行期還是同一個字串，但編譯期會被當成不同型別。',
        '兩個只有搭配在一起才有意義的欄位（例如一個「是否仍活躍」的布林值，加上一個只有在不活躍時才有意義的「結束年份」），應該合併成一個「可能有值/可能沒值」的欄位，而不是兩個鬆散關聯的獨立參數——這樣就從根本上排除了「活躍中卻同時帶著一個結束年份」這種矛盾組合的存在可能。',
        '一個範圍有限的固定選項集合（例如音樂類型）應該用封閉的聯合型別/enum 來建模，而不是自由格式的字串，這樣打錯字或給了不存在的類型時編譯器會直接拒絕，而不是默默接受。',
        '把「聯合型別（tagged union）」跟「多欄位的組合型別」搭配使用，可以組出「代數資料型別」：例如一段活躍期間可以建模成剛好兩種帶標籤的變體之一——「從某年至今仍活躍」或「在某年到某年之間活躍」——取代原本模糊的「Option+布林」組合，而且之後每個要處理這個型別的函式都會被要求（透過窮舉式的 pattern matching／switch）明確處理這兩種情況，不能漏掉。',
        '需求／行為本身也可以被建模成資料：與其讓一個函式帶一堆鬆散相關的布林/陣列參數（類型篩選、地點篩選、活躍年份篩選，每個都可能不啟用），不如定義一個「搜尋條件」的聯合型別，再傳入一串這種條件——把原本冗長的參數列表變成一個可以隨意擴充、意圖清楚的條件清單。',
      ],
    },
    {
      levelId: 'gfp-8',
      keyPoints: [
        '把一個會讀寫外部世界的不純函式（呼叫 API、讀寫終端機輸入輸出）包裝起來（概念上類似 JS 裡用一個 thunk 或尚未 await 的 Promise 描述「之後才要執行的動作」），包裝的當下並不會真的執行它，而只是建立一個值，描述「這是一個之後執行時會產生某個結果的計算」，真正的副作用被延後到程式裡明確呼叫一次「執行」的地方才會發生。',
        '這種「延遲計算」的容器本身也有 map／flatMap，所以多個包好的 IO 步驟（例如分別取兩個人的行事曆再合併）可以用前面章節同一套 for 迴圈式管線串接起來——串接的過程完全不會真的執行任何副作用，只有在最後把整條組合值拿去執行時才會發生。',
        '只要用到一個回傳 IO 的函式，呼叫端自己的回傳型別也會被迫變成 IO（書裡稱為 IO 有「傳染性」，類似 JS 裡 async 函式不管內部做什麼、回傳的一定是 Promise）——這讓型別簽名一眼就能看出程式裡哪些地方可能有副作用、哪些地方保證是純的。',
        '重試一次、重試後改用預設值等復原策略，是透過在 IO 值上串接 .orElse(...) 來表達——跟第六章 Option／Either 的 orElse 是同一套語彙，不需要巢狀的 try/catch，讓重試/備援邏輯可以宣告式地跟主要商業邏輯分開放。',
        '「重試 N 次」的通用輔助函式、以及把一串各自獨立的 IO 呼叫（例如每個與會者各打一次 API）合併成單一個回傳整批結果陣列的 IO，做法都是把 IO 值本身當成陣列元素，用 map／foldLeft／sequence 去摺疊——書裡從頭到尾用在一般陣列上的同一套工具，原封不動可以套用在「一串待執行的 IO 計算」上。',
        '整體設計目標（書裡稱為「functional core」）是把所有真正會產生副作用的呼叫盡量往程式的邊緣推，核心邏輯只用純函式組成、副作用實作透過參數傳進去（依賴注入的概念），這樣核心邏輯完全不需要真的執行任何 I/O 就能被測試與推理。',
      ],
    },
    {
      levelId: 'gfp-9',
      keyPoints: [
        'Stream 描述一段可能無限的資料序列，概念上類似 JS 的 generator 或惰性序列：只有消費端真正要求值時（例如 take(n)）才會產生資料，跟「值已經算好放著」的陣列或 Promise 完全不同。',
        '書中用線上換匯範例：不知道要打幾次 API 才會出現「連續 n 次匯率上升」的趨勢，於是改用遞迴不斷呼叫 API，靠遞迴的 base case（找到趨勢）結束；沒找到趨勢前，程式描述的是一個可能永遠跑不完的動作，但因為惰性求值，建立這個描述本身不會卡住或當機。',
        '用「滑動視窗」取代「每次固定抓 n 筆做一次判斷」的批次判斷法——批次法會漏掉恰好跨在兩個批次之間的趨勢，滑動視窗每多一筆新資料就重新檢查最近 n 筆，不浪費任何已經打過的 API 呼叫。',
        '用兩條 stream 的 zip 做「固定速率」節流：把資料 stream 和一條「每隔 1 秒 tick 一次」的計時器 stream 對齊，兩者中較慢的一方決定合併後的節奏，不用手動寫 setTimeout 或計數器邏輯。',
        '不可變的 Map／tuple 搭配 zip、drop 可以優雅表示「本期與前一期」的配對關係，取代直接用 list 索引硬查的寫法，讓程式碼能處理任意長度的輸入而不是寫死長度假設。',
        'Stream 中單一步驟失敗會讓整條 stream 失敗，可以用類似 Promise 的 orElse／重試策略復原，讓「取得資料」跟「判斷趨勢」這兩個關注點徹底分離，各自獨立可測試。',
      ],
    },
    {
      levelId: 'gfp-10',
      keyPoints: [
        'Ref 類比一個「執行緒安全的可變盒子」：更新內容不是直接賦值，而是傳入一個「舊值→新值」的純函式，底層用類似 compare-and-swap 的機制自動偵測衝突並重試，不需要手動上鎖。',
        'fiber（輕量執行緒）比系統原生執行緒輕很多，可以大量建立、多個 fiber 共享同一條系統執行緒；parSequence 類似把一堆非同步任務同時丟出去、等全部完成的 Promise.all，但失敗處理和資源回收都內建在裡面。',
        '城市打卡排行榜範例：一個 fiber 持續處理進來的打卡資料、另一個 fiber 定期讀資料算排行榜，兩者透過同一個 Ref 共享狀態，不需要傳統的鎖或訊息佇列，就避免了資料被同時讀寫造成的競爭問題。',
        '用「回傳型別標示成不會產生值」的手法（書中的 IO[Nothing]／foreverM）明確表示某段邏輯設計上就是無限迴圈（例如持續更新排行榜），讓型別本身說明這個函式的執行特性，不用靠註解或猜測。',
        '把同步阻塞呼叫改成非同步設計：函式不再是「執行完才能拿到結果」，而是立刻回傳一個「控制代碼」，代碼上有「讀最新結果」與「喊停」兩個動作，使資料產生與資料消費的步調完全解耦，呼叫方可以自己決定多久讀一次。',
      ],
    },
    {
      levelId: 'gfp-11',
      keyPoints: [
        '設計流程先把需求中的名詞（景點、地點、藝人、電影）轉成不可變的資料模型，再把「查詢動作」寫成一組只有函式簽名、還沒有實作的介面（書中稱為 DataAccess，類似先定義一組尚未實作的資料存取介面），讓商業邏輯可以先寫先測，完全不受資料來源影響。',
        '採用「由上而下設計＋盡量延後決定」：先確定最上層函式的簽名與呼叫方式，直到最後一刻才決定實際要串接哪個資料來源或函式庫，換資料來源時商業邏輯完全不用改。',
        '就算要串接的是「不純」的第三方命令式函式庫（書中用 Java 的 Apache Jena 呼叫 Wikidata 為例），做法是把每個不安全呼叫都包進 IO（對應 JS 就是包成回傳 Promise 的函式），讓外部命令式程式碼跟自己的商業邏輯純函式徹底隔離，避免「不純」擴散到整個系統。',
        '用 Resource 這種型別明確處理「用完一定要釋放」的資源（連線、檔案控制代碼等），以 acquire/use/release 三段式取代手動 try/finally，即使中途丟例外也保證資源會被釋放，避免連線洩漏。',
        '效能優化分兩層：先把彼此獨立的查詢從循序改成平行（sequence 改 parSequence，類似把連續 await 換成 Promise.all），再加一層快取（用類似 Ref 的可變參照存查過的結果）；因為介面本來就設計得夠乾淨，加快取只需要換掉底層一個小函式，商業邏輯完全不用動。',
      ],
    },
    {
      levelId: 'gfp-12',
      keyPoints: [
        '純函式的測試就是「給輸入、斷言輸出」，不需要任何 mock library：因為函式不依賴外部狀態，同樣輸入永遠得到同樣輸出，測試只是單純呼叫函式加上斷言。',
        'Property-based testing（性質測試）：不寫死少數幾組例子，而是描述「不管輸入是什麼，輸出都必須符合的規則」（例如某個評分函式的結果永遠落在某個範圍），讓測試框架自動產生大量隨機輸入（含邊界值）去驗證——書中就是靠這個技巧才真的抓到一個整數溢位（overflow）的真實 bug，是先前手寫範例測試完全沒發現的。',
        '測試涉及 IO／副作用的函式時分兩種策略：一種是把外部資料源「寫死回傳固定值」（對應 JS 就是傳入回傳固定內容的假函式），用來驗證「資料被怎麼使用」；另一種是接一個真正的本地測試伺服器，用來驗證「跟外部服務的請求/回應格式是否正確」，兩者驗證的目標不同，不能互相取代。',
        '判斷要用哪種測試策略的準則是「這個需求的責任在自己的程式碼、還是外部服務本身」——例如排序如果本來就是外部服務做的，測試就不必驗證排序結果，只需驗證自己的程式碼有沒有正確使用回傳的資料。',
        '用測試驅動開發示範「紅－綠－重構」流程：先為還沒實作的新需求（例如失敗時要回傳整理過的錯誤報告，而不是讓整個流程直接失敗）寫一支會失敗的測試，再寫最簡單能通過測試的實作，最後重構抽出更乾淨的輔助函式，重複這個循環直到需求都被涵蓋。',
      ],
    },
  ],
  sicp: [
    {
      levelId: 'sicp-1-1',
      keyPoints: [
        '數字等基本表達式可用運算子（如 + 、 * ）組成複合表達式（組合式），JavaScript 採中序記法並遵守運算子優先權與左結合規則，複合表達式還能無限層層嵌套。',
        'const 宣告是最簡單的抽象手段：把運算結果的值跟一個名稱綁在一起存進「環境」，之後就能用名稱代替重複描述細節。',
        '求值運算子組合式的規則本身是遞迴的——先求值每個運算元、再套用運算子——可以畫成樹狀圖表示數值由末端節點往上層層累積的過程。',
        '函式宣告（如 square、sum_of_squares）把一段複合運算包裝成一個可重複使用的單元；理解函式套用的初步方法是代換模型：把參數逐一替換成引數再化簡表達式，但這只是輔助思考的簡化模型，不代表直譯器真正的運作方式。',
        '牛頓法求平方根的例子（sqrt_iter、improve、is_good_enough）示範函式可以當「黑盒子」使用：只要回傳結果一致，函式內部怎麼實作、參數叫什麼名字（區域變數的作用域／bound name）都不影響使用者，這也是把輔助函式包進區塊結構、隱藏在外層函式內部的動機。',
        'JavaScript 採用「先求值引數再套用函式」的 applicative-order evaluation，不同於「完全展開表達式後才化簡」的 normal-order evaluation；兩者對能用代換模型描述的合法運算會得到相同結果，但求值的中間過程與重複運算次數不同。',
      ],
    },
    {
      levelId: 'sicp-1-2',
      keyPoints: [
        '函式在語法上是否呼叫自己（遞迴函式）跟它實際產生的計算「過程形狀」是兩回事：階乘的第一種寫法產生線性遞迴過程（先展開成一串延遲運算鏈再逐步收縮，所需空間隨 n 線性增長），第二種用 product、counter 等狀態變數逐步更新的寫法則產生線性迭代過程（狀態可用固定數量變數完整描述）。',
        '費氏數列的直觀遞迴寫法會產生樹狀遞迴：每次呼叫都分裂成兩個子呼叫，形成一棵樹，步驟數隨 n 呈指數成長，但因為只需記住目前在樹上的路徑，空間只隨樹的深度線性成長；同一問題改用三個狀態變數（a、b、count）迭代，就能把指數步驟壓成線性步驟。換零錢問題（count_change）是另一個容易描述、卻同樣有大量重複運算的樹狀遞迴例子。',
        '用 Θ 記號描述的成長階（order of growth）可以粗略比較不同演算法消耗的時間、空間資源如何隨問題規模 n 增長，例如線性 Θ(n)、指數 Θ(φⁿ)、對數 Θ(log n)，讓人一眼看出演算法規模放大後的差異。',
        '快速冪 fast_expt 利用「連續平方」的技巧，把計算 bⁿ 所需的乘法次數從線性的 Θ(n) 降到對數的 Θ(log n)，展現同一個數學問題換一種分解方式就能大幅改變效率等級。',
        '歐幾里得演算法（輾轉相除法求 GCD）是一個步驟數隨輸入呈對數成長的迭代過程，書中並藉由 Lamé\'s Theorem 把它的步驟數上限跟費氏數列的成長速度連結起來。',
        '質數測試比較了逐一嘗試因數的方法（成長階 Θ(√n)）與機率性的費馬測試（利用費馬小定理與連續平方計算模冪 expmod，成長階只有 Θ(log n)）；後者只能保證「未通過測試必非質數」，通過測試只是高機率而非絕對保證，是機率演算法的代表例子。',
      ],
    },
    {
      levelId: 'sicp-1-3',
      keyPoints: [
        'sum_integers、sum_cubes、pi_sum 三個函式除了「每項要加什麼」跟「下一項怎麼取」不同，其餘結構完全一樣；把這個共同樣板抽出來、將相異之處變成參數，就得到通用的高階函式 sum(term, a, next, b)——函式可以接受函式當引數，這就是高階函式（higher-order function）的核心概念，integral（數值積分）等更複雜的運算可以直接疊在 sum 之上建構出來。',
        'lambda 表達式（例如 x => x + 4）讓人不必為了配合高階函式而特地命名一堆只用一次的小函式，可以直接就地寫出匿名函式；同樣地，需要區域性的中間變數時，用 const 在函式內宣告會比另外包一層輔助函式更直接。',
        '條件表達式（? :）跟條件語句（if/else）的差別在於：條件語句允許在分支內用 const 宣告只在該分支求值的區域變數，這在 expmod 的例子中避免了提早求值導致的重複計算、甚至無窮遞迴的陷阱。',
        'half-interval method（二分逼近法）跟 fixed-point search（不動點搜尋）都是把「求解的一般性方法」直接寫成函式的例子；書中發現求平方根其實等價於尋找 y = x/y 的不動點，但直接搜尋會在兩個值之間來回震盪不收斂，需要用 average damping（把新舊猜測值取平均）才能讓逼近過程穩定收斂。',
        'average_damp、deriv（數值微分）都是「輸入一個函式、回傳另一個新函式」的例子；牛頓法可以表示成對某個變換函式（newton_transform）做不動點搜尋，結果發現前面兩種求平方根的方法（不動點搜尋＋平均阻尼、牛頓法）本質上是同一個更高層抽象 fixed_point_of_transform 的兩個特例。',
        '本節最後點出「第一級（first-class）地位」的概念：JavaScript 的函式可以被命名、當引數傳遞、當回傳值、存進資料結構，正是因為函式擁有這種不受限制的地位，才使得前面這些把函式當資料操作的高階抽象成為可能。',
      ],
    },
    {
      levelId: 'sicp-2-1',
      keyPoints: [
        '資料抽象方法論：先用「一廂情願」假設 make_rat/numer/denom 這些建構子與選擇器已經存在，用它們定義 add_rat 等運算，最後才決定有理數的具體表示法(pair)——先定義行為介面，再補實作。',
        'pair/head/tail 是唯一需要的「膠水」，可以把兩個值黏成一個複合物件，且能巢狀使用(pair 裡面再放 pair)，就能組出任意複雜的結構。',
        '抽象屏障(abstraction barrier)把系統分層，每層只透過上一層定義的操作跟下一層溝通(使用者用 add_rat 等 -> 用 make_rat/numer/denom -> 用 pair/head/tail)，即使把約分時機從建構時改到選取時，上層完全不用修改。',
        '「資料是什麼」的定義不是看實作方式，而是看建構子與選擇器要滿足的條件(例如 head(pair(x,y)) 要等於 x)；書中示範用純函式(內部 dispatch)也能實作出滿足這個條件的 pair，證明資料抽象比表面上看到的資料結構更抽象。',
        '區間運算(interval arithmetic)的延伸練習顯示：即使兩個公式在代數上等價(例如並聯電阻的兩種寫法)，因為不確定性傳播的方式不同，計算出的區間寬度也會不同，同一個不確定變數在公式中重複出現越多次，結果誤差通常越大。',
      ],
    },
    {
      levelId: 'sicp-2-2',
      keyPoints: [
        'pair 的封閉性質(closure property)：組合出來的東西(pair)本身還可以再被當作組合的原料，遞迴套用就能建出串列(list)與樹狀結構，這是資料能無限疊加複雜化的關鍵。',
        'list 是用 null 結尾的巢狀 pair 鏈，靠 head/tail 逐步往下拆解，是 list_ref、length、append 等函式共同的遞迴走訪模式；用同樣的方式走訪樹，只是多了一個「這個分支本身可能又是一棵子樹」的判斷(is_pair)，例如 count_leaves。',
        'map/filter/accumulate(即 fold_right)把「列舉→過濾→轉換→累加」這條處理管線抽出來變成可重組的標準零件，讓兩個表面上完全不同的遞迴函式(例如 sum_odd_squares 和 even_fibs)都能改寫成同一套訊號流程樣板，凸顯程式的意圖而不是實作細節。',
        'flatmap 把巢狀迴圈(例如列舉所有數對、產生排列組合、八皇后擺放)表達成「對每個元素 map 出一串結果，再用 append 全部攤平」，把巢狀迴圈也收編進序列運算的框架裡。',
        '繪圖語言(painter)把「圖像」表示成一個接受 frame、把自己畫進去的函式；beside/below/flip_vert 這些組合子也滿足封閉性質(組合出來的還是 painter，可以再被組合)，配合 transform_painter 統一處理座標變換，示範資料抽象與高階函數如何撐出一個能疊代生成複雜圖案(如 square_limit)的迷你語言，也帶出分層設計(stratified design)的概念。',
      ],
    },
    {
      levelId: 'sicp-2-3',
      keyPoints: [
        '符號微分程式(deriv)示範同一套資料抽象策略：先用 is_sum/addend/augend/make_sum 等抽象操作寫出微分規則本身，不管代數式究竟怎麼表示；之後才決定用前綴記法的巢狀 list(如 list("+", "x", 3))來實作這些選擇器/建構子，deriv 本身完全不用改動。',
        '微分結果預設不會自動化簡，改善方式跟有理數約分如出一轍：不動 deriv，只改 make_sum/make_product 這兩個建構子，讓它們在建構當下就處理 0、1 等特例並直接做數字運算，上層演算法完全保持不變。',
        '集合的三種表示法有不同效能特徵：無序 list 的成員檢查/交集是 Θ(n)/Θ(n²)；改成有序 list 後 is_element_of_set 平均可以提早中止、交集降到 Θ(n)；改成二元搜尋樹能把搜尋/插入降到 Θ(log n)，但代價是樹必須維持平衡，否則(例如依序插入 1~7)會退化成鏈狀失去優勢。',
        'Huffman 編碼是一種變動長度的前綴碼(prefix code)：常出現的符號給較短的位元序列，且任何符號的編碼都不會是另一個符號編碼的開頭，讓解碼時不需要分隔符也能從位元序列唯一還原訊息。',
        'Huffman 樹的生成演算法是每次從目前的葉節點/子樹集合中挑出權重最小的兩個合併成新節點(權重相加)，重複直到只剩一棵樹；編碼/解碼則是沿著樹從根往下走，左右分支分別對應位元 0/1，走到葉節點就得到一個符號。',
      ],
    },
    {
      levelId: 'sicp-2-4',
      keyPoints: [
        '複數可以用直角座標(實部/虛部)或極座標(長度/角度)表示，兩種表示法對不同運算(加減 vs 乘除)各有優勢；為了讓兩種表示法能在同一系統中並存，需要幫每個資料物件貼上型別標籤(type tag，如 "rectangular"/"polar")，用 attach_tag/type_tag/contents 存取。',
        '最初的做法是讓每個通用選擇器(如 real_part)內部用條件式檢查 tag、分派到對應實作，這種寫法有兩個缺點：每次新增一種表示法都要回頭修改所有通用選擇器，而且不同表示法的函式名稱必須手動避免撞名。',
        '資料導向程式設計(data-directed programming)把「運算名稱 × 型別」這個二維表格用 put/get 實際做出來；每種表示法各自包成一個 install_xxx_package 函式，把自己的實作登記進表格，新增表示法只需要新增登記、完全不用修改既有的通用函式，達到可加性(additivity)。',
        'apply_generic 是通用的分派機制：取出參數的型別標籤，用「(運算名稱, 型別)」去表格查出對應函式並套用到剝除標籤後的內容上；這讓 real_part、add 這類通用函式的定義本身完全不必知道底層有哪些表示法存在。',
        '訊息傳遞(message passing)是另一種組織方式：把資料物件本身寫成一個依「操作名稱」分派行為的函式(dispatch)，等於把「運算 × 型別」表格按型別拆成一欄一欄、各自包在資料建構子裡，而不是按運算拆成一列列的通用選擇器。',
      ],
    },
    {
      levelId: 'sicp-2-5',
      keyPoints: [
        '把 add/sub/mul/div 都透過 apply_generic 依參數型別分派到各自的套件(原生數字、有理數、複數...)，讓同一個 add 能同時處理原生數字、有理數、複數，是資料導向設計思路的延伸應用；複數套件本身還套用了兩層標籤("complex" 外層再包 "rectangular"/"polar" 內層)。',
        '混合型別運算(例如複數加原生數字)如果為每個型別組合都手寫一個對應函式，數量會隨型別數量平方成長且難以維護；改用強制轉型(coercion)機制——用 put_coercion/get_coercion 建立型別間的轉換表，讓 apply_generic 查不到直接對應運算時嘗試把其中一個參數轉型成另一個的型別再重試，只需要 N² 個轉換函式而非每個運算都重寫。',
        '如果型別之間存在「塔狀」的自然階層(整數⊂有理數⊂實數⊂複數)，可以定義 raise 把型別逐級往上提升到共同層級再運算，也可以定義 project/drop 把結果盡量降到最簡單的型別(例如 6+0i 化簡回整數 6)；但像幾何圖形這種一個型別有多個父型別/子型別的情況，就無法套用簡單塔狀結構，型別階層設計本身變成困難問題。',
        '多項式套件(install_polynomial_package)展示可加性(additivity)的威力：add_poly/mul_poly 內部合併同次項係數時呼叫的是通用的 add/mul，如果係數本身又是多項式(例如 x 的多項式、係數是 y 的多項式)，就會遞迴呼叫回 add_poly/mul_poly 本身，形成「資料導向的遞迴」，完全不用為多層巢狀係數另外寫程式碼。',
        '項次列表(term list)依多項式是稀疏還是稠密，分別採用不同表示法——稠密多項式適合用「串列位置代表次方」的簡單表示，稀疏多項式改用(次方,係數)配對只存非零項，避免存一大堆 0；有理函式(多項式的分數)要化簡到最簡分數，需要用歐幾里得演算法算多項式的最大公因式(GCD)，但實務上會遇到中間係數暴增，須用「偽除法(pseudodivision)」等技巧處理。',
      ],
    },
    {
      levelId: 'sicp-3-1',
      keyPoints: [
        '以 make_withdraw／make_account 為例：把 balance 宣告在外層函式中，回傳的內層函式透過閉包存取它，外界只能經由呼叫函式間接改動，無法直接碰到這個變數，這就是「區域狀態」的封裝方式。',
        '一旦引入賦值，替代模型（substitution model）就失效了：make_simplified_withdraw(25)(20) 這種同一運算式呼叫兩次會得到不同結果，函式不再等同於單純的數學函式，需要新的求值方式來解釋（也就是後面 3.2 的環境模型）。',
        '賦值破壞了「referential transparency」：D1、D2 兩個 make_decrementer(25) 產生的函式可以互相替換而不影響結果，但 W1、W2 兩個 make_simplified_withdraw(25) 產生的函式卻各自累積不同的提款紀錄，不能互相替換，凸顯「相等」在有狀態物件上變得模糊。',
        '命令式（imperative）寫法暗藏排序陷阱：把遞迴版 factorial 改寫成用 counter、product 兩個變數逐步賦值時，兩行賦值的先後順序若寫反就會算錯，這種「要注意賦值順序」的問題在純函數式寫法裡完全不會發生。',
        '用 rand（封裝 local state 的隨機數產生器）取代直接呼叫 rand_update 來寫 Monte Carlo 估計 π：前者可以把「目前推進到哪個亂數」這件事完全藏在 rand 內部，monte_carlo 函式因此可以維持通用、不需要知道亂數細節，展示了隱藏狀態對模組化的實際好處。',
      ],
    },
    {
      levelId: 'sicp-3-2',
      keyPoints: [
        '環境（environment）是一串 frame 串起來的鏈：每個 frame 是名稱對應數值的表格，並有指標指向外層環境；查詢名稱時沿鏈往外找第一個有綁定的 frame，內層的綁定可以遮蔽（shadow）外層同名的綁定。',
        '函式在此模型中是「程式碼＋建立時所在環境」的組合；呼叫函式會新建一個 frame 把參數綁定到引數值，而這個新 frame 的外層環境固定是函式物件自帶的那個環境，不是呼叫端當時的環境——這正是詞法作用域（lexical scope）成立的原因。',
        '用 make_withdraw(100) 建立 W1、W2 兩個帳號物件說明：兩者共用同一段程式碼，但各自呼叫時建立了不同的 frame（各自的 balance），因此彼此的狀態互不干擾——frame 本身就是儲存區域狀態的「地方」。',
        '賦值 name = value 的規則是：沿環境鏈找到第一個含有該名稱綁定的 frame 去修改它；若該綁定是常數（用 := 標示）就報錯，若整條鏈都找不到這個名稱則是未宣告錯誤。',
        '區塊（block）進入時會建立新 frame，收納區塊內直接宣告的名稱（宣告前存取會因暫時性死區 TDZ 而出錯）；這讓 sqrt 內部的 is_good_enough、improve、sqrt_iter 這類局部函式可以互相呼叫甚至互相遞迴，同時不會外洩到外層命名空間。',
      ],
    },
    {
      levelId: 'sicp-3-3',
      keyPoints: [
        'set_head、set_tail 讓 pair 可以被就地修改而非只能組出新結構，這也讓「共享」變得可被觀察到：z1 = pair(x, x) 的 head 與 tail 指向同一個 pair，修改其中一個會連帶影響另一個，可以用 === 偵測這種共享，跟各自獨立建構出來的 z2 行為完全不同。',
        '佇列（queue）用一對指標 front_ptr／rear_ptr 表示，插入時靠 set_tail 直接接到尾端指標所在位置，把原本用一般 list 表示、插入要掃到底（Θ(n)）的操作降為常數時間（Θ(1)）。',
        '表格（table）用帶著虛擬表頭記錄（如 "*table*"）的 headed list 表示，靠 assoc 尋找、set_tail 就地新增或更新記錄；二維表格則是「表中又有子表格」的巢狀結構，make_table 用閉包把 local_table 藏起來，讓 get／put 共享同一份隱藏狀態。',
        '數位電路模擬器（inverter、and_gate、half_adder、full_adder）把每條 wire 實作成帶 signal_value 與 action_functions 兩個可變狀態的物件，再靠 agenda（事件佇列＋after_delay／propagate）驅動事件在指定延遲後才觸發，示範可變物件如何配合訊息傳遞去建模有時間延遲的並行事件。',
        '約束傳播網路（如攝氏—華氏轉換器）用 connector 物件（可變的 value／informant／constraints）實作雙向可逆的計算：同一張網路既可以由 C 算出 F，也可以反過來由 F 算回 C，跟一般函式只能單向計算的性質完全不同。',
        '書中指出 mutation 本質上就是賦值：即使把 pair 改用純函式＋dispatch 的方式實作，只要內部用 let 搭配賦值紀錄 x、y 的值，就足以重現 set_head／set_tail 的所有效果，說明賦值與可變資料在理論上可以互相化約。',
      ],
    },
    {
      levelId: 'sicp-3-4',
      keyPoints: [
        '核心問題是多個 thread 共享同一個狀態變數：balance = balance - amount 這行其實包含「讀取、計算、寫回」三個步驟，Peter 與 Paul 兩個提款 thread 若交錯執行這三步驟，可能讓帳戶餘額從 100 變成 75 而非正確的 65，破壞了金額守恆（圖 3.29 的經典例子）。',
        '「正確」的併行不代表完全禁止同時執行，而是要求結果等同於某種循序執行的順序（可能不只一種合法結果），甚至有更寬鬆的情境（例如熱擴散模擬）完全不需要限制執行順序，最終仍會收斂到正確答案。',
        'serializer（序列化器）機制：用 make_serializer 把一組函式標記為「同一時刻只能有一個在執行」，內部靠共享的 mutex 做 acquire／release；只序列化單一帳戶自己的存提款，就能避免競態，但不同帳戶之間仍可以自由並行。',
        '交換兩個帳戶餘額（exchange）的例子顯示只序列化各自帳戶還不夠：必須用兩個帳戶各自的 serializer 一起保護整個交換函式（serialized_exchange），才能避免中途被其他人插入修改——這說明「共享多個資源」遠比「共享單一資源」複雜。',
        'deadlock（死結）：Peter 交換 a1/a2、Paul 同時交換 a2/a1，兩人各自握著一把鎖等待對方釋放另一把鎖，永遠僵持不下；一種避免方式是替資源編號，規定所有 thread 一律先嘗試取得編號較小的鎖。',
        'mutex 底層仰賴 test_and_set 這種必須「不可分割」（atomic）的操作，否則連 mutex 自己都可能出現跟銀行帳戶一樣的競態；書末也點出，若沒有 thread 之間的溝通機制，「同一時刻的共享狀態」這個概念本身其實就沒有明確定義。',
      ],
    },
    {
      levelId: 'sicp-3-5',
      keyPoints: [
        'stream 的核心結構是 pair(h, () => t)：把 tail 包成一個延遲求值的無參數函式（thunk），只有呼叫 stream_tail 去 force 它時才真正算出下一個元素，因此可以表示無窮序列（如 integers、fibs），也能像質數篩選那樣只計算真正需要的部分，避免 list 版本要先構造整份再丟棄大部分的浪費。',
        'memo 這個記憶化技巧把 delay 包裝成「第一次 force 後就快取結果」的版本（call-by-need），避免同一段 stream 被重複計算多次，是把 1.2.2 節記憶化表格的想法自動套用到 stream 的 tail 上。',
        'stream 可以「隱式」自我參照地定義，例如 integers 定義成自己加上 ones、或用 sieve 對 stream 逐層濾掉目前質數的倍數來產生無窮質數 stream；靠 tail 的延遲求值，這種自我參照的定義才不會變成無窮遞迴當機。',
        '合併兩個無窮 stream（例如所有整數配對）要用 interleave 交替取元素，而不能用 stream_append，因為 append 會想先把第一個無窮 stream 取完才輪到第二個，導致第二個 stream 的元素永遠排不上。',
        'streams as signals：把訊號處理系統（integral、RC 電路、用 solve 求解微分方程）表示成 stream 之間互相參照的網路；當網路裡有回授迴圈時，被積量（integrand）必須以「延遲引數」的形式傳入 integral，否則會卡在還沒定義好就要先求值的循環相依。',
        'stream 提供不靠賦值也能建模「看似有狀態」系統的方式：stream_withdraw 本身是純函式（輸入餘額與提款金額 stream，輸出餘額 stream），對使用者而言像是在跟一個狀態隨時間變化的物件互動，但實作裡完全沒有賦值或區域變數；書末也提醒，若要合併多個使用者各自的請求 stream（如聯名帳戶），又會把 functional 風格原本想迴避的同步問題整個帶回來。',
      ],
    },
    {
      levelId: 'sicp-4-1',
      keyPoints: [
        'evaluate 與 apply 兩個函式互相遞迴構成求值核心循環：evaluate 依語法類型分派、算出運算元後交給 apply；apply 遇到 primitive 函式直接呼叫底層 JS，遇到 compound 函式則在延伸環境中對函式本體求值，兩者反覆呼叫直到化簡為名稱查詢與基本呼叫。',
        'parse 先把程式文字轉成 tagged list（例如 list("literal", value)、list("application", ...)），evaluate 完全透過抽象的 is_xxx 判斷式與 selector 存取這些結構而不寫死具體語法，因此換一套語法函式就能求值完全不同的語言。',
        '環境用一串 frame 串成的鏈結表示，compound 函式物件本身攜帶宣告當時的環境，apply 時再疊上一個綁定參數的新 frame——這正是環境模型把「函式」與「定義時環境」綁在一起、實現閉包的機制。',
        '區塊求值前會先 scan_out_declarations，把區塊內所有宣告名稱一次綁定為特殊值 "*unassigned*"，讓同一區塊裡的函式（如互相遞迴的 is_even／is_odd）能彼此參照，模擬 JS 區塊作用域「同時生效」而非「循序生效」的規則。',
        '4.1.7 把 evaluate 拆成只做一次語法分析、回傳等待環境的 execution function 的 analyze，以及之後可反覆呼叫的執行階段，避免像 factorial(4) 這種重複呼叫時每次都重新判斷語法結構，是用「分析與執行分離」換取效能的經典手法。',
        '函式呼叫的回傳值用特殊的 return_value 包裝物件標記，讓 eval_sequence／apply 能分辨函式是提早 return 還是執行完最後一行，這是這個直譯器處理「函式呼叫該回傳什麼」的關鍵機制。',
      ],
    },
    {
      levelId: 'sicp-4-2',
      keyPoints: [
        'JS 是應用序（applicative-order）語言，呼叫前會先求出所有引數值；惰性求值屬於正常序，延後到真正需要值的那一刻才求值，因此像 try_me(0, head(null)) 這種原本會報錯的呼叫，在惰性求值下可以正常返回而不出錯。',
        '引數改用 thunk（未求值運算式＋所在環境包裝成的物件）延遲傳遞，只有在被 primitive 函式使用、當作條件判斷的 predicate、或被當作函式呼叫時才會用 force_it 強制求值，這個過程稱為 forcing。',
        '直譯器對 thunk 採 memoization：force_it 第一次求值後把 thunk 原地改寫成已求值的 evaluated_thunk 存下結果，之後再次 force 同一個 thunk 不必重算，兼顧惰性與效率。',
        '主要改動集中在函式呼叫：compound 函式維持 non-strict（不先求值引數就進入函式本體），primitive 函式仍是 strict 要先求出引數值；只有在需要真正取值的地方（如條件判斷）才把 evaluate 換成會強制求值的 actual_value。',
        '把 pair 改用函式閉包實作（pair 回傳一個接收 selector 的函式）而非內建結構，就能讓 pair 本身也是 non-strict 的，讓 stream 與一般 list 合而為一，不需要像第三章那樣另外實作一整套 stream 專用操作。',
        '惰性 pair 比第三章的 stream 更「懶」：不只尾端被延遲，連頭元素本身也要用到才強制求值，因此可以直接遞迴定義 integral／solve 這類含迴圈依賴的無窮數列，不必額外插入 delay 表達式。',
      ],
    },
    {
      levelId: 'sicp-4-3',
      keyPoints: [
        '引入新語法 amb(e1,...,en)，代表「模糊地」回傳其中一個值；amb() 沒有任何選項，代表計算直接失敗，require 正是靠「條件不成立就呼叫 amb()」實作出的斷言機制。',
        '直譯器採深度優先、按順序回溯：遇到 amb 先選第一個選項，之後若 require 失敗就自動退回最近一個還有其他選項的選擇點嘗試下一個，選項用盡才再往上一層回溯，這套自動搜尋是這個評估器最核心的差異之處。',
        '像 prime_sum_pair、office_move 這類程式先用 amb 選出候選值、再用 require 過濾，把窮舉加過濾的邏輯寫成宣告式風格，搜尋機制完全交給直譯器，程式本身不必手寫回溯邏輯。',
        '實作上把分析求值器每個 execution function 的參數從單一環境擴充成環境、success continuation（帶著值與失敗時要做什麼的 continuation）、failure continuation（不帶引數、代表換下一個選擇），靠這兩種 continuation 的傳遞實現選擇與回溯。',
        '賦值成功後，success continuation 會把「回復舊值」包進新的 failure continuation 裡；之後若某處觸發回溯，會先復原這次賦值再往上一層傳遞失敗，讓 side effect 能隨著回溯自動撤銷。',
        'driver loop 支援 retry：第一次求值只印出第一個不失敗的結果，之後輸入 retry 會呼叫上次 success continuation 帶出來的 next_alternative（其實就是那次選擇點留下的失敗延續）繼續搜尋下一組解，直到所有分支都失敗為止。',
      ],
    },
    {
      levelId: 'sicp-4-4',
      keyPoints: [
        '查詢語言把知識表達成關聯而非單向函式：append_to_form 只用兩條規則，就能同時回答「兩表附加結果為何」「附加誰能得到某結果」「所有能附加成某結果的組合」這三種不同方向的問題，不像 JS 的 append 函式只能單向計算。',
        '核心機制是模式比對與 unification 的分工：比對資料庫裡具體的斷言用單向的 pattern_match，比對規則（規則的 conclusion 兩邊都可能含變數）則需要對稱的 unify_match，能同時推導出變數彼此之間的對應值。',
        'and／or／not 三種組合方式都用「frame 的 stream」實作：and 把前一個查詢的輸出 frame stream 串接餵給下一個查詢做級聯過濾，or 對同一輸入平行求值後合併結果，not 則對每個輸入 frame 嘗試求值其內容、只留下求不出解的 frame。',
        '套用規則的方式呼應 apply 套用函式：先 unify 查詢樣式與規則結論得到擴充後的 frame，再相對這個新 frame 求值規則主體，對應到「綁定參數到引數再求值函式本體」的評估—應用循環，顯示查詢語言與 JS 直譯器共享同一套底層結構。',
        '每次套用同一條規則前都要 rename_variables_in，把規則裡的變數換成獨一無二的新名稱（附加一個 rule_application_id），避免同一條規則被多次套用時彼此的變數被誤認成同一個。',
        '這套語言的 not 高度依賴程序性順序而非純邏輯：它只能過濾已綁定的變數、代表「資料庫中無法證明」而非真正的邏輯否定（closed world assumption），加上規則遞迴（如 married 的對稱規則）容易造成無窮迴圈，凸顯「宣告式外觀、程序式底層」語言的侷限。',
      ],
    },
    {
      levelId: 'sicp-5-1',
      keyPoints: [
        '設計暫存器機器要分兩部分：資料路徑（registers、operations、可推動資料流動的 button）決定「能做什麼」，控制器（一串帶標籤的指令＋分支）決定「按什麼順序做」；書中用 GCD 演算法（a、b 兩個暫存器＋一個暫存用的 t，配合 rem 運算與 b=0 測試）示範這兩層如何合起來描述一台機器。',
        '暫存器機器語言把資料路徑的定義直接內嵌進控制器指令裡（例如 assign、test、branch、go_to），好處是不用來回對照 button／operation 名稱定義，缺點是描述變長、也讓人容易忘記這些指令並非任意的 JavaScript 運算，只能操作暫存器內容與常數。',
        '多處需要同一種計算（例如機器裡有兩段 GCD 運算）時，與其複製一份資料路徑，不如共用同一組硬體、讓控制序列跳到同一段子程式再跳回來；一開始用固定的返回位置處理，後來改良成用一個 continue 暫存器存放「返回時要跳去的標籤」，這樣多個呼叫點才能共用同一段子程式邏輯。',
        '疊代型計算（如迭代版 GCD）只需要固定數量的暫存器反覆覆寫即可完成；但遞迴型計算（如 factorial、雙重遞迴的 Fibonacci）在展開子問題時，舊的暫存器內容之後還要用到，因此需要一個「後進先出」的堆疊，在呼叫前 save 需要保留的暫存器（例如 n、continue），呼叫後再依相反順序 restore，藉此把無限層的巢狀機器壓縮成有限硬體＋一個可伸縮的堆疊。',
        'factorial 機器示範堆疊如何實作遞迴：呼叫前把 n 與 continue 存進堆疊、遞迴完成後恢復並用 val 暫存器帶回結果；Fibonacci 機器則因為一次要遞迴呼叫兩次（fib(n-1) 和 fib(n-2)），需要在每次呼叫前後都存好之後還要用到的值，展示了更複雜的巢狀子程式呼叫模式。',
      ],
    },
    {
      levelId: 'sicp-5-2',
      keyPoints: [
        '模擬器對外只提供四個介面函式：make_machine 依暫存器名稱、運算表、控制器描述建出機器模型；set/get_register_contents 存取暫存器；start 從控制序列開頭執行到結尾，讓使用者不必手動逐步模擬指令就能測試機器設計是否正確。',
        '機器模型內部用訊息傳遞（message-passing）風格實作：暫存器是帶有 get/set 訊息的小型物件，堆疊是用 pair 串起來、支援 push/pop/initialize 的物件，pc（program counter）暫存器決定下一步要執行哪條指令，flag 暫存器記錄最近一次測試的真偽供 branch 判斷。',
        '組譯（assemble）分兩階段進行：extract_labels 先掃過整個控制序列，把字串標籤和真正的指令分開，同時建出「標籤→序列位置」的對照表；之後 update_insts 才替每條指令產生對應的執行函式，確保產生執行函式時所有標籤都已經知道位置，不會有前向參照的問題。',
        '每條指令被轉換成一個「執行函式」（不帶參數、呼叫它就等於執行這條指令並自動推進 pc），這跟第 4.1.7 節把語法分析與求值分離的做法如出一轍：查找暫存器、解析運算元這些「分析」工作只在組譯時做一次，之後模擬執行同一指令再多次也不用重複分析，是這個模擬器效能來源。',
        '運算式（operation expression，如 list(op("rem"), reg("a"), reg("b"))）在模擬時透過 apply_in_underlying_javascript，把對應的 JavaScript 函式實際套用到暫存器裡目前的值上，讓抽象的機器運算能借助真正的 JavaScript 執行環境完成。',
        '模擬器可以加裝效能監控：修改堆疊實作以記錄推入次數與最大堆疊深度，這個機制在後面 5.4、5.5 節被用來實際比較直譯器與編譯器對同一段程式（例如 factorial）的堆疊使用量差異。',
      ],
    },
    {
      levelId: 'sicp-5-3',
      keyPoints: [
        '串列結構在實際記憶體裡用兩個平行的向量（the_heads、the_tails）表示：一個 pair 指標其實就是這兩個向量的共同索引，head/tail/pair 這些操作都可以改寫成對這兩個向量做 vector_ref/vector_set，並用一個 free 暫存器記錄下一個可用位置。',
        '為了讓 === 保持「單純比較指標是否相同」這種簡單語意，系統對字串採用 interning：維護一個字串池，遇到內容相同的字串就回傳同一個既有指標，而不是每次都配置新的字元序列。',
        '垃圾回收的判準是可達性：任何時刻只有從目前暫存器出發、透過一連串 head/tail 能走到的 pair 才可能影響後續計算，走不到的記憶格就是可以回收的垃圾。',
        '本節採用的 stop-and-copy 演算法把記憶體分成工作區與空間區兩半：工作區用滿時，從 root（代表所有暫存器內容）出發，把所有可達的 pair 依序搬到空間區並壓縮排列，接著互換兩半角色，繼續配置在原本的空間區，讓下一輪垃圾回收有新的工作區可用。',
        '搬移過程靠 free（下一個空位）與 scan（下一個待掃描的已搬移 pair）兩個指標推進，並在原位置留下「broken heart」標記加轉發位址，確保同一個共用子結構被多個指標指到時只搬一次、之後改指向新位置即可，直到 scan 追上 free 為止。',
        '相較於另一種常見做法 mark-sweep（要走訪並標記整塊記憶體、再整片掃過一遍才能回收），stop-and-copy 只碰觸真正存活的資料、且順帶完成記憶體壓縮，對大型且有虛擬記憶體分頁機制的系統更有利，但代價是任何時刻都要保留兩倍空間。',
      ],
    },
    {
      levelId: 'sicp-5-4',
      keyPoints: [
        '把第 4 章 metacircular 直譯器的 evaluate／apply 遞迴呼叫，改寫成一台有七個暫存器（comp 存待求值元件、env 存環境、val 存結果、continue 存返回位置、fun／argl／unev 用於函式呼叫）加一個堆疊的具體機器，讓原本隱藏在 JavaScript 呼叫堆疊裡的控制流程完全攤開成看得見的暫存器操作與 save/restore。',
        'eval_dispatch 對元件語法類型做 case 分析，跟 metacircular evaluate 的結構完全對應，每種語法（literal、name、lambda、conditional、sequence、block、return、declaration、assignment）各自對應一段獨立的控制器入口。',
        '求值函式呼叫時，continue、env、unev、argl、fun 要在求值函式運算式與每個引數運算式前後適當地 save/restore，並對最後一個引數做特殊處理省下一次不必要的堆疊操作（evlis tail recursion），這跟直觀上「每次都存好所有東西」的做法相比更精簡。',
        '透過 push_marker_to_stack 與 revert_stack_to_marker 機制，在 ev_return 求值 return 運算式前先把堆疊還原到函式呼叫當時的狀態，讓「函式呼叫後直接是 return 的呼叫」不會在堆疊上累積任何多餘資訊——這是這個直譯器具備尾遞迴（tail-recursive）特性的關鍵，跟 metacircular 直譯器必須先包裝、之後再檢查 return 值、因而不斷堆疊資訊形成鮮明對比。',
        'compound_apply 用函式的參數與 argl 中的實際引數擴展環境後求值函式本體，並把 continue 預設指向 return_undefined，因此函式本體如果沒有明確執行 return 就結束，回傳值自然是 undefined。',
        '可以用第 5.2 節的堆疊統計功能實際跑這台機器，量出遞迴 factorial 與疊代 factorial 在堆疊深度上的差異（疊代版深度固定、遞迴版隨 n 增長），把第 1、5.1 節裡「疊代 vs 遞迴」的抽象概念變成可以實測驗證的數字。',
      ],
    },
    {
      levelId: 'sicp-5-5',
      keyPoints: [
        '編譯器 compile 函式的語法 case 分析結構跟直譯器的 eval_dispatch 幾乎一樣，但差別在於：直譯器每次執行都要重新判斷元件類型、重新分析語法，編譯器只在編譯當下分析一次，之後產生的目的碼（object code）可以重複執行任意次而不用再分析——這是編譯比直譯快的根本原因。',
        '每個程式碼產生器除了元件本身，還額外帶兩個參數：target（結果要放進哪個暫存器）與 linkage（執行完後要「接著下一條指令」「跳回 continue」還是「跳到某個標籤」），讓同一段元件的編譯邏輯可以視所在的呼叫情境彈性接續不同後續動作。',
        '組合指令序列時用 append_instruction_sequences 單純串接，但涉及暫存器保存時改用 preserving：它會分析兩段序列各自「需要」與「會修改」哪些暫存器，只有前段真的會覆寫、且後段真的需要該值時才插入 save/restore，不像直譯器保守地每次都全部存起來，因此能省下大量不必要的堆疊操作。',
        '編譯出的函式呼叫（compile_fun_appl）沿用跟顯式控制直譯器一樣的 push_marker_to_stack／continue 暫存器機制，並在 target 是 val、linkage 是 return 時刻意不多做一次 continue 的 save，讓編譯出的尾呼叫同樣能以常數堆疊空間執行；書中實測直譯執行 factorial(5) 需要 151 次堆疊推入、最大深度 28，同一函式編譯後只需要 36 次推入、最大深度 14，具體展示了這些優化帶來的效率差距。',
        '5.5.6 節的詞法定址（lexical addressing）進一步優化名稱查找：因為語言是詞法作用域，編譯器可以在編譯時就靠一個「編譯期環境」推算出某個名稱在執行期會落在往外數第幾層 frame、第幾個位置，用固定的 (frame 數, 位移) 兩個數字直接存取，取代逐層逐名稱比對的 lookup_symbol_value 搜尋。',
        '5.5.7 節示範如何讓編譯碼與直譯器整合成一個系統：compile_and_go 把編譯結果組譯成指令、灌進直譯器機器的 val 暫存器並設定 flag 跳到特別的 external_entry 入口執行，使得編譯過的函式與直譯執行的函式可以在同一個執行環境裡互相呼叫。',
      ],
    },
  ],
}

export const getChapterSummary = (chapterId: string): LevelSummary[] | undefined =>
  chapterSummaries[chapterId]

// 重點文字沒有天生的唯一 id，用「關卡 id + 陣列索引」組出穩定識別碼給收藏功能存
export const keyPointId = (levelId: string, index: number): string => `${levelId}:${index}`

export interface SavedKeyPoint {
  id: string
  text: string
  chapterId: string
  chapterTitle: string
  levelTitle: string
}

// 收藏重點清單用：只存 id（Record<string, boolean>），畫面顯示的文字/章節資訊都即時從
// chapterSummaries／chapters 查回來，不用在收藏紀錄裡另外存一份文字快照
export const getSavedKeyPoints = (savedKeyPointIds: Record<string, boolean>): SavedKeyPoint[] => {
  const result: SavedKeyPoint[] = []
  for (const chapter of chapters) {
    const summaries = chapterSummaries[chapter.id]
    if (!summaries) continue
    for (const summary of summaries) {
      const level = chapter.levels.find((l) => l.id === summary.levelId)
      summary.keyPoints.forEach((text, index) => {
        const id = keyPointId(summary.levelId, index)
        if (savedKeyPointIds[id]) {
          result.push({ id, text, chapterId: chapter.id, chapterTitle: chapter.title, levelTitle: level?.title ?? summary.levelId })
        }
      })
    }
  }
  return result
}
