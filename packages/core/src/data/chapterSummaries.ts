import { chapters } from './chapters'

// 章節重點整理：給「複習」分頁用，內容改寫自各書章節，不是題目本身
// 目前只有 gfp（Grokking Functional Programming）驗證過，其餘章節之後陸續補上
export interface LevelSummary {
  levelId: string
  keyPoints: string[]
}

export const chapterSummaries: Record<string, LevelSummary[]> = {
  gfp: [
    {
      levelId: 'gfp-1',
      keyPoints: [
        '函式可以想成一個「黑盒子」：外部只看得到簽名（輸入型別、輸出型別），身體（實作邏輯）藏在盒子裡。FP 特別看重簽名要能完整交代這個函式在做什麼，讓使用者不用打開盒子看實作就能安心使用。',
        '「函式會說謊」：像 getFirstCharacter 拿到空字串會丟例外、divide 除以 0 會炸掉，這些函式的簽名（承諾回傳一個值）跟實際行為（可能整個中斷）對不上，這種落差正是程式最容易出錯的地方。',
        '命令式（imperative）著重「怎麼做」的每一步驟（例如用 for 迴圈逐字元累加分數）；宣告式（declarative）著重「要什麼」（例如直接用 word.length 描述分數即為字數）。書中把同一個計分需求分別用兩種風格實作，對照凸顯宣告式通常更精簡、更貼近需求描述。',
        '本書把函式式程式設計簡化定義成兩個核心特質：簽名不說謊、函式主體盡量宣告式——後面所有章節都是圍繞這兩點展開。',
      ],
    },
    {
      levelId: 'gfp-2',
      keyPoints: [
        '書中用購物車折扣範例示範不純函式的地雷：class 內部用 items 陣列和 bookAdded 布林值當狀態，addItem/removeItem 會偷偷改動這些狀態，一旦呼叫端直接操作 getItems() 回傳的陣列（例如直接呼叫 remove），或忘記同步更新 bookAdded，折扣計算就會跟真實購物車內容脫節、產生「幽靈折扣」。',
        '純函式的三條判斷準則：只回傳單一值、只根據參數計算結果（不偷看外部狀態）、不修改任何既有的值。書中逐步把 getDiscountPercentage 從讀取 bookAdded 狀態，改成每次都重新掃描 items 陣列即時計算（recalculating instead of storing），再把 items 直接當參數傳入，最終瘦身成一個不依賴任何內部狀態的靜態函式。',
        'referential transparency（參照透明）：如果一個函式呼叫可以整個替換成它的回傳值而不改變程式行為（例如 f(20) 永遠能被 19 取代），就代表這個函式是可信賴、可預測的——這是純函式最重要的實務好處之一。',
        'side effect（副作用）泛指函式除了計算回傳值以外做的任何事：發 HTTP 請求、印出訊息、寫資料庫、丟例外、甚至只是修改一個全域變數都算，純函式的定義就是完全不做這些事。',
        '純函式的測試特別直白：只要呼叫函式比對回傳值即可（例如 getDiscountPercentage([\'Book\']) === 5），不需要像不純版本那樣得先建立物件、呼叫好幾個方法設定內部狀態才能斷言——這也是為什麼純函式被視為更好維護、更好測試的基礎。',
      ],
    },
    {
      levelId: 'gfp-3',
      keyPoints: [
        '本章用歐洲旅行規劃的 replan(plan, newCity, beforeCity) 範例揭穿一個隱藏陷阱：即使函式回傳了正確的新清單 planB，只要它在實作裡直接對傳入的原始陣列做插入（mutate），呼叫端手上的 planA 也會被悄悄改掉——單看函式簽名完全看不出這個副作用，必須翻開實作才知道它不是純函式。',
        '修法是「複製再修改」：函式一開始就用傳入的陣列建立一份全新拷貝，所有插入/刪除都只作用在這份拷貝上，回傳這份拷貝，原始參數保持原封不動——重點是純函式可以自由修改自己在函式內部新建立的值，禁止的只是修改「從外部傳進來」的既有值。',
        '書中另一個賽車圈速範例（totalTime/avgTime 都對同一個 lapTimes 陣列呼叫 remove(0)）示範了共享可變狀態（shared mutable state）最陰險的一面：每個函式單獨測試都正確，兩個函式合在一起呼叫時卻因為互相修改到同一份陣列而得出錯誤的平均值，說明「狀態被多處共享並可修改」的風險會隨程式規模疊加，難以在小範圍測試中發現。',
        '解決共享可變狀態有三條路：程式碼複製後修改（前面用的手法）、物件導向的封裝（把可變狀態包在 class 內部、只透過方法存取，但要小心洩漏內部參照）、以及函式式的做法——乾脆換成不可變值，讓資料一旦建立就永遠無法被改變，任何「修改」都得產生一份新值。',
        '本章把函式式程式設計濃縮成一句定義：用純函式操作不可變值的程式設計方式——純函式是引擎，不可變值就是驅動這個引擎的燃料。',
      ],
    },
    {
      levelId: 'gfp-4',
      keyPoints: [
        '書中用單字排名需求（rankedWords）示範函式簽名如何從「說謊」一路修到「誠實」：一開始把排序邏輯（scoreComparator）寫在函式外部當隱藏依賴，函式簽名完全看不出它靠什麼排序；改成把比較邏輯／計分函式當作參數傳進來後，只看簽名就能知道這個函式的排序方式是可以替換的——這正是把函式當成值傳遞的核心價值。',
        'map、filter、foldLeft 都是「高階函數」（higher-order function：接受函式當參數，或回傳函式的函式）在 JS 裡的直接對應：map 對陣列每個元素套用同一個轉換函式產生等長新陣列；filter 用回傳布林值的判斷函式挑出符合條件的元素；foldLeft（對應 JS 的 reduce）則是帶著一個累加值，逐一走訪陣列元素把結果收斂成單一值，三者都不修改原陣列。',
        '函式可以回傳函式：當一個函式需要固定住某些參數（例如評分演算法、清單）、只留一個參數（例如門檻值）晚點才決定時，可以讓外層函式回傳一個只接收剩餘參數的內層函式，避免每次呼叫都要重複傳同一組固定參數。',
        'currying（柯里化）就是把一個多參數函式，轉換成一連串每次只接收一個參數、彼此回傳彼此的單參數函式鏈（在 JS 就是連續回傳箭頭函式，如 a => b => c => 結果）；重點不是語法炫技，而是讓呼叫端可以在程式的不同地方分別決定每個參數，減少重複傳遞相同引數的樣板碼。',
        'product type（乘積型別，Scala 用 case class 表達，對應到 JS 大致就是一個描述固定欄位形狀的物件，例如 { name, year }）用來把兩個以上相關聯的資料綁在一起變成一個不可變值；由於它本身不可變，跟 map／filter 這類高階函數搭配時特別安全，可以直接用箭頭函式取出欄位（例如 lang => lang.name）而不用擔心欄位被意外修改。',
      ],
    },
    {
      levelId: 'gfp-5',
      keyPoints: [
        '把一個陣列裡每個元素都轉成另一個陣列再攤平，會得到「陣列的陣列」；flatten 負責把這層巢狀攤平成一層，而 flatMap 就是「先 map 再 flatten」的合併版，而且輸出陣列的長度可以跟輸入不一樣（可以變短、變空、也可以變長），這點是 map 做不到的。',
        'flatMap 有一個特性：不管串接（chain）呼叫還是巢狀（nest）呼叫，只要每一步都回傳同一種容器型別，結果完全等價；也因為這個特性，才能安全地把一串 flatMap 改寫成巢狀寫法。書裡把這件事列為全書最重要的觀念之一。',
        '串接式的 flatMap 每一步只看得到「上一步」傳進來的值，前面步驟的中間變數會超出作用域；改成巢狀寫法可以讓所有前面算出來的值同時留在作用域內，這在最後一步需要同時用到好幾個前面步驟的值時（例如同時要用到書、作者、電影三個值）是必要的，但巢狀寫法會犧牲可讀性。',
        'for 迴圈式的語法糖（概念上類似 JS 裡把一串 promise 鏈改寫成 async/await 讓巢狀變好讀）在編譯期會被機械式地展開成巢狀的 flatMap／map 呼叫；當一次迭代多種集合型別（例如同時迭代陣列跟集合）時，最終回傳的容器型別是由「第一個」被迭代的集合型別決定，後面的都會被轉型去配合它。',
        '在這種鏈式寫法裡做篩選有三種手法：先對來源呼叫 filter、寫一個內嵌的條件判斷、或是寫一個函式在條件不成立時回傳空陣列、成立時回傳單一元素陣列——第三種手法比較通用，不只能用在陣列，任何「有 flatMap」的型別都適用，這替後面用同一套機制處理錯誤處理埋下伏筆。',
        '像 Option 這種「可能有值（Some）也可能沒值（None）」的容器一樣有 flatMap，所以同一套管線寫法可以直接拿來做資料驗證/解析：每個驗證步驟各自回傳 Option，串在一起後只要任何一步失敗（回傳 None），整個計算就會短路直接回傳 None，完全不用寫顯式的 null 檢查。',
      ],
    },
    {
      levelId: 'gfp-6',
      keyPoints: [
        '回傳 null 或丟例外會讓函式的型別簽名說謊（宣稱一定回傳某個值，實際上有時候什麼都給不出來甚至讓程式崩潰）；改用 Option 之後，「可能失敗」這件事本身就變成回傳型別的一部分，呼叫端沒辦法假裝看不到這個可能性。',
        '因為 Option 本身有 flatMap，所以多個各自驗證一個欄位的函式可以串成一條管線：只要其中一步回傳 None，整條計算立刻短路回傳 None，不用往下繼續執行——跟陣列 flatMap 的短路機制其實是同一套原理。',
        'checked exception 只是「讓你有機會處理」錯誤，卻不容易組合：想串接一個主要流程和一個備援流程，通常得手動巢狀 try-catch。Option／Either 提供了 orElse——一個單純接兩個「可能失敗的值」、回傳其中先成功那個的函式——組合起來乾淨很多，要疊多層備援策略只要一直串接 .orElse() 就好。',
        '處理一個「每個元素都可能各自失敗」的陣列時，書裡點名兩種策略：「盡力而為」（每個元素轉成 Option，最後只收集成功的，失敗的默默丟掉）跟「全有全無」（把整批 Option 摺疊成單一個 Option／Either，只要有一個失敗整批就算失敗）。',
        'Option 的弱點是失敗時只會給你一個 None，看不出到底哪裡出錯；Either（一個不是「成功值」就是「帶有說明文字的失敗值」的容器）解決了這個問題——你已經學過的 map／flatMap／orElse 這套語彙，原封不動可以用在 Either 上，只是失敗的那一邊多帶了一段描述訊息。',
        '把一條基於 Option 的驗證管線改寫成 Either，幾乎只是機械式替換：None 換成 Left("錯誤說明")、Some 換成 Right，管線的結構完全不用變——證明同一套模式一旦學會，換到另一個容器型別幾乎可以直接照搬。',
      ],
    },
    {
      levelId: 'gfp-7',
      keyPoints: [
        '只用字串、數字、布林這些原始型別建模業務資料，會讓編譯器接受各種不合理的組合（參數順序放反、一個布林旗標搭配一個只有旗標為真/假時才有意義的欄位、或是一個打錯字的「類型」字串）——型別系統完全幫不上忙，資料有效性只能全靠寫程式的人自己在每個用到資料的地方手動檢查。',
        '把某個字串包成一個「零成本包裝型別」（newtype，概念上類似 JS 裡用一個很輕量的 wrapper/branded type 來標記型別），可以避免不小心把兩個型別相同、意義不同的參數傳反（例如把地點誤填到類型的位置）——執行期還是同一個字串，但編譯期會被當成不同型別。',
        '兩個只有搭配在一起才有意義的欄位（例如一個「是否仍活躍」的布林值，加上一個只有在不活躍時才有意義的「結束年份」），應該合併成一個「可能有值/可能沒值」的欄位，而不是兩個鬆散關聯的獨立參數——這樣就從根本上排除了「活躍中卻同時帶著一個結束年份」這種矛盾組合的存在可能。',
        '一個範圍有限的固定選項集合（例如音樂類型）應該用封閉的聯合型別/enum 來建模，而不是自由格式的字串，這樣打錯字或給了不存在的類型時編譯器會直接拒絕，而不是默默接受。',
        '把「聯合型別（tagged union）」跟「多欄位的組合型別」搭配使用，可以組出「代數資料型別」：例如一段活躍期間可以建模成剛好兩種帶標籤的變體之一——「從某年至今仍活躍」或「在某年到某年之間活躍」——取代原本模糊的「Option+布林」組合，而且之後每個要處理這個型別的函式都會被要求（透過窮舉式的 pattern matching／switch）明確處理這兩種情況，不能漏掉。',
        '需求／行為本身也可以被建模成資料：與其讓一個函式帶一堆鬆散相關的布林/陣列參數（類型篩選、地點篩選、活躍年份篩選，每個都可能不啟用），不如定義一個「搜尋條件」的聯合型別，再傳入一串這種條件——把原本冗長的參數列表變成一個可以隨意擴充、意圖清楚的條件清單。',
      ],
    },
    {
      levelId: 'gfp-8',
      keyPoints: [
        '把一個會讀寫外部世界的不純函式（呼叫 API、讀寫終端機輸入輸出）包裝起來（概念上類似 JS 裡用一個 thunk 或尚未 await 的 Promise 描述「之後才要執行的動作」），包裝的當下並不會真的執行它，而只是建立一個值，描述「這是一個之後執行時會產生某個結果的計算」，真正的副作用被延後到程式裡明確呼叫一次「執行」的地方才會發生。',
        '這種「延遲計算」的容器本身也有 map／flatMap，所以多個包好的 IO 步驟（例如分別取兩個人的行事曆再合併）可以用前面章節同一套 for 迴圈式管線串接起來——串接的過程完全不會真的執行任何副作用，只有在最後把整條組合值拿去執行時才會發生。',
        '只要用到一個回傳 IO 的函式，呼叫端自己的回傳型別也會被迫變成 IO（書裡稱為 IO 有「傳染性」，類似 JS 裡 async 函式不管內部做什麼、回傳的一定是 Promise）——這讓型別簽名一眼就能看出程式裡哪些地方可能有副作用、哪些地方保證是純的。',
        '重試一次、重試後改用預設值等復原策略，是透過在 IO 值上串接 .orElse(...) 來表達——跟第六章 Option／Either 的 orElse 是同一套語彙，不需要巢狀的 try/catch，讓重試/備援邏輯可以宣告式地跟主要商業邏輯分開放。',
        '「重試 N 次」的通用輔助函式、以及把一串各自獨立的 IO 呼叫（例如每個與會者各打一次 API）合併成單一個回傳整批結果陣列的 IO，做法都是把 IO 值本身當成陣列元素，用 map／foldLeft／sequence 去摺疊——書裡從頭到尾用在一般陣列上的同一套工具，原封不動可以套用在「一串待執行的 IO 計算」上。',
        '整體設計目標（書裡稱為「functional core」）是把所有真正會產生副作用的呼叫盡量往程式的邊緣推，核心邏輯只用純函式組成、副作用實作透過參數傳進去（依賴注入的概念），這樣核心邏輯完全不需要真的執行任何 I/O 就能被測試與推理。',
      ],
    },
    {
      levelId: 'gfp-9',
      keyPoints: [
        'Stream 描述一段可能無限的資料序列，概念上類似 JS 的 generator 或惰性序列：只有消費端真正要求值時（例如 take(n)）才會產生資料，跟「值已經算好放著」的陣列或 Promise 完全不同。',
        '書中用線上換匯範例：不知道要打幾次 API 才會出現「連續 n 次匯率上升」的趨勢，於是改用遞迴不斷呼叫 API，靠遞迴的 base case（找到趨勢）結束；沒找到趨勢前，程式描述的是一個可能永遠跑不完的動作，但因為惰性求值，建立這個描述本身不會卡住或當機。',
        '用「滑動視窗」取代「每次固定抓 n 筆做一次判斷」的批次判斷法——批次法會漏掉恰好跨在兩個批次之間的趨勢，滑動視窗每多一筆新資料就重新檢查最近 n 筆，不浪費任何已經打過的 API 呼叫。',
        '用兩條 stream 的 zip 做「固定速率」節流：把資料 stream 和一條「每隔 1 秒 tick 一次」的計時器 stream 對齊，兩者中較慢的一方決定合併後的節奏，不用手動寫 setTimeout 或計數器邏輯。',
        '不可變的 Map／tuple 搭配 zip、drop 可以優雅表示「本期與前一期」的配對關係，取代直接用 list 索引硬查的寫法，讓程式碼能處理任意長度的輸入而不是寫死長度假設。',
        'Stream 中單一步驟失敗會讓整條 stream 失敗，可以用類似 Promise 的 orElse／重試策略復原，讓「取得資料」跟「判斷趨勢」這兩個關注點徹底分離，各自獨立可測試。',
      ],
    },
    {
      levelId: 'gfp-10',
      keyPoints: [
        'Ref 類比一個「執行緒安全的可變盒子」：更新內容不是直接賦值，而是傳入一個「舊值→新值」的純函式，底層用類似 compare-and-swap 的機制自動偵測衝突並重試，不需要手動上鎖。',
        'fiber（輕量執行緒）比系統原生執行緒輕很多，可以大量建立、多個 fiber 共享同一條系統執行緒；parSequence 類似把一堆非同步任務同時丟出去、等全部完成的 Promise.all，但失敗處理和資源回收都內建在裡面。',
        '城市打卡排行榜範例：一個 fiber 持續處理進來的打卡資料、另一個 fiber 定期讀資料算排行榜，兩者透過同一個 Ref 共享狀態，不需要傳統的鎖或訊息佇列，就避免了資料被同時讀寫造成的競爭問題。',
        '用「回傳型別標示成不會產生值」的手法（書中的 IO[Nothing]／foreverM）明確表示某段邏輯設計上就是無限迴圈（例如持續更新排行榜），讓型別本身說明這個函式的執行特性，不用靠註解或猜測。',
        '把同步阻塞呼叫改成非同步設計：函式不再是「執行完才能拿到結果」，而是立刻回傳一個「控制代碼」，代碼上有「讀最新結果」與「喊停」兩個動作，使資料產生與資料消費的步調完全解耦，呼叫方可以自己決定多久讀一次。',
      ],
    },
    {
      levelId: 'gfp-11',
      keyPoints: [
        '設計流程先把需求中的名詞（景點、地點、藝人、電影）轉成不可變的資料模型，再把「查詢動作」寫成一組只有函式簽名、還沒有實作的介面（書中稱為 DataAccess，類似先定義一組尚未實作的資料存取介面），讓商業邏輯可以先寫先測，完全不受資料來源影響。',
        '採用「由上而下設計＋盡量延後決定」：先確定最上層函式的簽名與呼叫方式，直到最後一刻才決定實際要串接哪個資料來源或函式庫，換資料來源時商業邏輯完全不用改。',
        '就算要串接的是「不純」的第三方命令式函式庫（書中用 Java 的 Apache Jena 呼叫 Wikidata 為例），做法是把每個不安全呼叫都包進 IO（對應 JS 就是包成回傳 Promise 的函式），讓外部命令式程式碼跟自己的商業邏輯純函式徹底隔離，避免「不純」擴散到整個系統。',
        '用 Resource 這種型別明確處理「用完一定要釋放」的資源（連線、檔案控制代碼等），以 acquire/use/release 三段式取代手動 try/finally，即使中途丟例外也保證資源會被釋放，避免連線洩漏。',
        '效能優化分兩層：先把彼此獨立的查詢從循序改成平行（sequence 改 parSequence，類似把連續 await 換成 Promise.all），再加一層快取（用類似 Ref 的可變參照存查過的結果）；因為介面本來就設計得夠乾淨，加快取只需要換掉底層一個小函式，商業邏輯完全不用動。',
      ],
    },
    {
      levelId: 'gfp-12',
      keyPoints: [
        '純函式的測試就是「給輸入、斷言輸出」，不需要任何 mock library：因為函式不依賴外部狀態，同樣輸入永遠得到同樣輸出，測試只是單純呼叫函式加上斷言。',
        'Property-based testing（性質測試）：不寫死少數幾組例子，而是描述「不管輸入是什麼，輸出都必須符合的規則」（例如某個評分函式的結果永遠落在某個範圍），讓測試框架自動產生大量隨機輸入（含邊界值）去驗證——書中就是靠這個技巧才真的抓到一個整數溢位（overflow）的真實 bug，是先前手寫範例測試完全沒發現的。',
        '測試涉及 IO／副作用的函式時分兩種策略：一種是把外部資料源「寫死回傳固定值」（對應 JS 就是傳入回傳固定內容的假函式），用來驗證「資料被怎麼使用」；另一種是接一個真正的本地測試伺服器，用來驗證「跟外部服務的請求/回應格式是否正確」，兩者驗證的目標不同，不能互相取代。',
        '判斷要用哪種測試策略的準則是「這個需求的責任在自己的程式碼、還是外部服務本身」——例如排序如果本來就是外部服務做的，測試就不必驗證排序結果，只需驗證自己的程式碼有沒有正確使用回傳的資料。',
        '用測試驅動開發示範「紅－綠－重構」流程：先為還沒實作的新需求（例如失敗時要回傳整理過的錯誤報告，而不是讓整個流程直接失敗）寫一支會失敗的測試，再寫最簡單能通過測試的實作，最後重構抽出更乾淨的輔助函式，重複這個循環直到需求都被涵蓋。',
      ],
    },
  ],
}

export const getChapterSummary = (chapterId: string): LevelSummary[] | undefined =>
  chapterSummaries[chapterId]

// 重點文字沒有天生的唯一 id，用「關卡 id + 陣列索引」組出穩定識別碼給收藏功能存
export const keyPointId = (levelId: string, index: number): string => `${levelId}:${index}`

export interface SavedKeyPoint {
  id: string
  text: string
  chapterId: string
  chapterTitle: string
  levelTitle: string
}

// 收藏重點清單用：只存 id（Record<string, boolean>），畫面顯示的文字/章節資訊都即時從
// chapterSummaries／chapters 查回來，不用在收藏紀錄裡另外存一份文字快照
export const getSavedKeyPoints = (savedKeyPointIds: Record<string, boolean>): SavedKeyPoint[] => {
  const result: SavedKeyPoint[] = []
  for (const chapter of chapters) {
    const summaries = chapterSummaries[chapter.id]
    if (!summaries) continue
    for (const summary of summaries) {
      const level = chapter.levels.find((l) => l.id === summary.levelId)
      summary.keyPoints.forEach((text, index) => {
        const id = keyPointId(summary.levelId, index)
        if (savedKeyPointIds[id]) {
          result.push({ id, text, chapterId: chapter.id, chapterTitle: chapter.title, levelTitle: level?.title ?? summary.levelId })
        }
      })
    }
  }
  return result
}
